# <img src="https://flagcdn.com/w20/kr.png" width="20" style="vertical-align: middle;"> JAVA AWS CI/CD 풀스택과정 - Web<br>
### <img src="https://flagcdn.com/w20/us.png" width="20" style="vertical-align: middle;"> JAVA AWS CI/CD Full-stack Course - Web<br>
### <img src="https://flagcdn.com/w20/jp.png" width="20" style="vertical-align: middle;"> JAVA AWS CI/CD フルスタック課程 - Web<br>

<br>
<br>

* **Progress** : <img src="https://img.shields.io/badge/Progress-60%25-yellow?style=flat-square" style="vertical-align: middle;">
* **Language** : <img src="https://img.shields.io/badge/Java_21-ED8B00?style=flat-square&logo=openjdk&logoColor=white" style="vertical-align: middle;"> <img src="https://img.shields.io/badge/HTML5-E34F26?style=flat-square&logo=html5&logoColor=white" style="vertical-align: middle;"> <img src="https://img.shields.io/badge/CSS3-1572B6?style=flat-square&logo=css3&logoColor=white"> <img src="https://img.shields.io/badge/JSP-007396?style=flat-square&logo=java&logoColor=white" style="vertical-align: middle;">
* **DataBase** : <img src="https://img.shields.io/badge/Oracle_21c-F80000?style=flat-square&logo=oracle&logoColor=white" style="vertical-align: middle;">
* **IDE** : <img src="https://img.shields.io/badge/Eclipse-2C2255?style=flat-square&logo=eclipseide&logoColor=white" style="vertical-align: middle;"> <img src="https://img.shields.io/badge/VS_Code-007ACC?style=flat-square&logo=visualstudiocode&logoColor=white" style="vertical-align: middle;"> <img src="https://img.shields.io/badge/SQL_Developer-F80000?style=flat-square&logo=oracle&logoColor=white" style="vertical-align: middle;"> <img src="https://img.shields.io/badge/DBeaver-382923?style=flat-square&logo=dbeaver&logoColor=white" style="vertical-align: middle;">

<br>
<br>

📅 학습 일지 (Study Log / 学習記録)
------------------------------

<br>
자세한 내용: <a href="https://ki-sd.tistory.com/category/WebStudy" target="_blank"><img src="https://img.shields.io/badge/Tistory-000000?style=flat-square&logo=tistory&logoColor=white&link=https://ki-sd.tistory.com/"></a>
<br>

#### 2026-04
| 날짜 | 학습 내용 (KR / US / JP) |
| :--- | :--- |
| **05-06** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> 데스크톱 환경(Swing)을 벗어나 본격적인 웹 개발(JSP)에 입문함. Bootstrap 프레임워크를 활용하여 브라우저 UI를 구성하고, 오라클 DB와 연동된 맛집(Food) 데이터를 스크립틀릿(`<% %>`)과 표현식(`<%= %>`)을 통해 HTML 태그에 동적으로 렌더링함. `request.getParameter()`를 이용해 URL 쿼리스트링을 제어하고 삼항 연산자를 활용한 웹 기반 페이징 로직을 완벽하게 구현함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Initialized the WebStudy repository, transitioning from desktop (Swing) to Web (JSP) development. Rendered dynamic food data from Oracle DB onto HTML using scriptlets (`<% %>`) and expressions (`<%= %>`), styling the UI with Bootstrap. Mastered web-based pagination logic by controlling URL query strings via `request.getParameter()` and applying ternary operators.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> デスクトップ環境（Swing）から本格的なWeb開発（JSP）へ入門。Bootstrapフレームワークを活用してブラウザのUIを構成し、Oracle DBと連携した飲食店（Food）データをスクリプトレット（`<% %>`）と式（`<%= %>`）を用いてHTMLタグに動的にレンダリング。`request.getParameter()`を利用したURLクエリ文字列の制御と三項演算子を活用し、Webベースのページングロジックを実装した。</li></ul> |
| **05-07** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> 웹의 동작 원리(Client-Server 구조, HTTP 상태 코드)와 웹 표준(HTML5, CSS3, ES6)의 핵심 개념을 정립함. 프론트엔드와 백엔드 언어들의 역할을 구분하고, HTML5 문서의 기본 뼈대(`<!DOCTYPE html>`, `<head>`, `<body>`) 구조를 분석함. 또한 `<h1>`~`<h6>`, `<p>`, `<br>`, `<pre>` 등의 단락 제어 태그와 `<strong>`, `<mark>`, `<sup>` 등 다양한 문자 제어 태그를 실습하며 정적 웹 UI 렌더링의 기초를 다짐.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Established the core concepts of web operation principles (Client-Server architecture, HTTP status codes) and web standards (HTML5, CSS3, ES6). Distinguished the roles of frontend and backend languages and analyzed the basic structure of an HTML5 document. Practiced paragraph control tags (`<h1>`-`<h6>`, `<p>`, `<br>`, `<pre>`) and character formatting tags (`<strong>`, `<mark>`, `<sup>`) to build a solid foundation in static web UI rendering.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> Webの動作原理（クライアント・サーバー構造、HTTPステータスコード）とWeb標準（HTML5、CSS3、ES6）の核心概念を確立。フロントエンドとバックエンド言語の役割を区別し、HTML5ドキュメントの基本構造を分析した。また、段落制御タグ（`<h1>`〜`<h6>`、`<p>`、`<br>`、`<pre>`）や多様な文字制御タグ（`<strong>`、`<mark>`、`<sup>`など）を実習し、静的Web UIレンダリングの基礎を固めた。</li></ul> |
| **05-08** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> HTML5의 핵심인 테이블(`colspan`/`rowspan`)과 목록 태그를 활용한 시맨틱 UI 설계부터, 사용자 입력을 처리하는 폼(Form) 컨트롤(`<input>`, `<select>`, `<fieldset>`)까지 총망라하여 학습함. 정적 웹의 한계를 넘어 JQuery를 도입해 사원 목록을 동적으로 필터링(`keyup`)하는 프론트엔드 제어 기법을 익히고, 카카오맵 Open API를 연동해 문자열 주소를 지도 마커로 변환하는 외부 데이터 통신 기법을 실습함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Comprehensively studied HTML5 by designing semantic UIs using tables (`colspan`/`rowspan`) and lists, and mastering form controls (`<input>`, `<select>`) for user input. Overcame static web limitations by introducing JQuery for dynamic frontend filtering (`keyup`) of employee lists. Additionally, integrated the Kakao Map Open API to convert string addresses into interactive map markers, practicing practical external data communication.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> HTML5の核心であるテーブル（`colspan`/`rowspan`）やリストタグを活用したセマンティックUI設計から、ユーザー入力を処理するフォーム（Form）コントロール（`<input>`、`<select>`）まで網羅的に学習。静的Webの限界を超え、JQueryを導入して社員一覧を動的にフィルタリング（`keyup`）するフロントエンド制御技法を習得し、カカオマップOpen APIを連動して文字列の住所を地図マーカーに変換する外部データ通信技法を実習した。</li></ul> |
| **05-11** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> HTML5의 시맨틱 마크업(표, 목록, 폼, 텍스트 제어)과 CSS3의 스타일링 기법을 결합하여 프론트엔드 UI/UX 설계의 기반을 완성함. 전체, 태그, 클래스(`.`), 아이디(`#`) 선택자의 우선순위를 이해하고, 속성 선택자(`[attr^=value]`)와 계층 선택자(자손 `>`, 후손, 동위 `+`, `~`)를 활용해 원하는 DOM 요소를 정교하게 타겟팅하는 기법을 마스터함. 또한 `:hover` 반응 선택자를 이용하여 테이블 행이나 이미지에 동적인 시각적 피드백을 부여하는 실무 퍼블리싱 스킬을 체화함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Completed the foundation of frontend UI/UX design by combining HTML5 semantic markup (tables, lists, forms, text formatting) with CSS3 styling techniques. Mastered targeting DOM elements precisely using universal, tag, class (`.`), and ID (`#`) selectors based on specificity rules, along with attribute selectors (`[attr^=value]`) and combinators (child `>`, descendant, sibling `+`, `~`). Applied `:hover` pseudo-classes to images and table rows to provide dynamic visual feedback, acquiring practical web publishing skills.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> HTML5のセマンティックマークアップ（テーブル、リスト、フォーム、テキスト制御）とCSS3のスタイリング技法を結合し、フロントエンドUI/UX設計の基盤を完成。全体、タグ、クラス（`.`）、ID（`#`）セレクタの優先順位を理解し、属性セレクタ（`[attr^=value]`）や階層セレクタ（子 `>`, 孫、兄弟 `+`, `~`）を活用して目的のDOM要素を精巧にターゲティングする技法をマスターした。また、`:hover` 疑似クラスを用いてテーブルの行や画像に動的な視覚的フィードバックを付与する実務パブリッシングスキルを体得した。</li></ul> |
| **05-12** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> CSS 박스 모델(margin, padding, border)과 고급 선택자(구조, 상태, 속성)를 총망라하여 반응형 프론트엔드 스타일링의 기초를 다짐. 나아가 순수 HTML의 정적 한계를 극복하기 위해 JSP(Java Server Pages)를 도입함. `empDAO`와 `empVO`를 통해 오라클 DB의 다중 테이블(EMP, DEPT) JOIN 쿼리 결과를 가져오고, 스크립틀릿(`<% %>`)과 표현식(`<%= %>`)을 사용해 브라우저 화면(HTML Table)에 동적으로 렌더링하는 풀스택 웹 아키텍처를 구현함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Mastered frontend styling basics by comprehensively covering the CSS Box Model (margin, padding, border) and advanced selectors (structural, pseudo-classes, attribute). Overcame the static limitations of HTML by introducing JSP (Java Server Pages). Implemented a full-stack architecture that fetches Oracle DB JOIN query results (EMP, DEPT) via DAO/VO patterns and dynamically renders them onto the browser using scriptlets (`<% %>`) and expressions (`<%= %>`).</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> CSSボックスモデル（margin、padding、border）および高度なセレクタ（構造、状態、属性）を網羅し、フロントエンドのスタイリングの基礎を確立。さらに純粋なHTMLの静的限界を克服するためJSP（Java Server Pages）を導入した。DAO/VOパターンを通じてOracle DBの複数テーブル（EMP、DEPT）JOINクエリ結果を取得し、スクリプトレット（`<% %>`）と式（`<%= %>`）を使用してWebブラウザの画面に動的レンダリングするフルスタックWebアーキテクチャを実装した。</li></ul> |
| **05-13** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> CSS 박스 모델(Margin, Padding, Border)의 시계 방향 단축 속기법과 가시성(`display`), 배경(`background`) 제어 속성을 심층적으로 마스터함. 순수 HTML/CSS의 정적인 한계를 JQuery의 상태 토글(`hide()`, `show()`)로 극복하여 아코디언(더보기) UI를 구현함. 백엔드 측면에서는 Model 1 아키텍처 기반의 맛집 포털 프로젝트를 고도화하여, Oracle DB의 `OFFSET FETCH`를 이용한 서버 사이드 페이징 로직과 Bootstrap 그리드 시스템을 결합함. 특히 카카오맵 Geocoder API를 도입해 DB에 저장된 텍스트 주소를 실시간 위경도 좌표로 변환하여 렌더링하는 클라이언트-서버-외부 API 간의 완벽한 3단 통신 시스템을 구축함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Mastered CSS Box Model shorthands (Margin, Padding, Border) and deeply explored visibility (`display`) and `background` properties. Overcame static limitations by implementing an accordion (Read More) UI using JQuery's state toggles (`hide()`, `show()`). On the backend, advanced a Food Portal project based on the Model 1 architecture, integrating server-side pagination (`OFFSET FETCH`) with the Bootstrap grid system. Notably, built a robust 3-tier communication system (Client-Server-External API) by integrating the Kakao Map Geocoder API to convert stored text addresses into real-time interactive map coordinates.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> CSSボックスモデルの短縮記法（Margin、Padding、Border）と可視性（`display`）、背景（`background`）制御属性を深層的にマスター。純粋なHTML/CSSの静的限界をJQueryの状態トグル（`hide()`、`show()`）で克服し、アコーディオン（もっと見る）UIを実装した。バックエンドの側面では、Model 1アーキテクチャベースの飲食店ポータルプロジェクトを高度化し、Oracle DBの`OFFSET FETCH`を利用したサーバーサイドページングロジックとBootstrapグリッドシステムを結合。特にカカオマップGeocoder APIを導入し、DBに保存されたテキスト住所をリアルタイムの緯度経度座標に変換してレンダリングする、クライアント・サーバー・外部API間の完璧な3層通信システムを構築した。</li></ul> |
| **05-14** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> CSS3의 가시성 속성(`display`, `visibility`, `opacity`)을 활용해 문서 객체 모델(DOM) 상의 공간 점유와 이벤트 수신 여부를 제어함. `text-overflow: ellipsis`와 `white-space`를 결합하여 맛집 및 음악 목록의 긴 텍스트를 깔끔하게 처리하는 실무 퍼블리싱 기법을 체화함. 백엔드 측면에서는 기존 JSP Model 1 구조를 확장하여 외부 API(KOBIS) 연동을 위한 '프록시(Proxy) 서버 패턴'을 도입함. Java(`MovieManager`)로 원격 데이터를 수집하고 JSP(`result.jsp`)가 중계하여 프론트엔드로 전달하는 파이프라인을 구축하며, 정적 화면을 넘어선 데이터 기반의 동적 웹 애플리케이션 아키텍처를 확립함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Mastered precise control over DOM spatial allocation and event reception using CSS3 visibility properties (`display`, `visibility`, `opacity`). Internalized practical publishing techniques by combining `text-overflow: ellipsis` and `white-space` to handle long text in Food/Music lists. On the backend, expanded the existing JSP Model 1 structure by introducing a 'Proxy Server Pattern' for external API (KOBIS) integration. Established a dynamic, data-driven web application architecture by building a pipeline where Java (`MovieManager`) collects remote data and JSP (`result.jsp`) relays it to the frontend.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> CSS3の可視性属性（`display`、`visibility`、`opacity`）を活用し、DOM上の空間占有とイベント受信を精密に制御。`text-overflow: ellipsis`と`white-space`を組み合わせ、飲食店や音楽一覧の長いテキストをきれいに処理する実務パブリッシング技法を体得した。バックエンドの側面では、既存のJSP Model 1構造を拡張し、外部API（KOBIS）連携のための「プロキシサーバーパターン」を導入。Java（`MovieManager`）でリモートデータを収集し、JSP（`result.jsp`）が中継してフロントエンドに伝達するパイプラインを構築し、静的画面を超えたデータ駆動型の動的Webアプリケーションアーキテクチャを確立した。</li></ul> |
| **05-15** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> JSP Model 1 아키텍처를 기반으로 웹 서비스의 핵심인 게시판 CRUD(생성, 조회, 수정, 삭제) 전체 사이클을 완벽하게 구현함. 데이터 입력 폼(View)과 DB 처리 전용 페이지(`_ok.jsp`)를 분리하여 초기 형태의 컨트롤러 패턴을 설계함. `SimpleDateFormat`을 활용해 당일 작성된 글에만 'NEW' 뱃지를 띄우는 동적 UI 로직과, JS `scrollHeight`를 이용한 텍스트에어리어 자동 크기 조절 등 실무적인 UX 디테일을 추가함. 또한 트랜잭션 성공 여부(`boolean`)에 따라 `sendRedirect`와 JS `history.back()`을 분기 처리하는 백엔드 라우팅 제어 기법을 확립함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Completely implemented the full CRUD (Create, Read, Update, Delete) cycle of a web bulletin board based on the JSP Model 1 architecture. Designed an early form of the controller pattern by separating data input forms (View) from DB processing pages (`_ok.jsp`). Added practical UX details, such as dynamically rendering a 'NEW' badge for posts created today using `SimpleDateFormat`, and auto-resizing textareas using JS `scrollHeight`. Established backend routing control techniques by branching `sendRedirect` and JS `history.back()` based on transaction success (`boolean`).</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> JSP Model 1アーキテクチャに基づき、Webサービスの中核である掲示板のCRUD（作成、参照、更新、削除）全サイクルを完璧に実装。データ入力フォーム（View）とDB処理専用ページ（`_ok.jsp`）を分離し、初期形態のコントローラーパターンを設計した。`SimpleDateFormat`を活用して当日に作成された記事にのみ「NEW」バッジを表示する動的UIロジックや、JSの`scrollHeight`を利用したテキストエリアの自動サイズ調整など、実務的なUXのディテールを追加。また、トランザクションの成功可否（`boolean`）に応じて`sendRedirect`とJS `history.back()`を分岐処理するバックエンドルーティング制御技法を確立した。</li></ul> |
| **05-18** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> 순수 Java 코드로 HTML을 그려내는 서블릿(Servlet) 방식의 한계를 실습하고, 이를 극복하기 위해 등장한 JSP의 내부 렌더링 생명주기(JSP &rarr; Servlet 변환 &rarr; 컴파일 &rarr; HTML 응답)를 구조적으로 완벽히 이해함. 스크립틀릿(`<% %>`)과 표현식(`<%= %>`)을 이용한 서버 사이드 데이터 바인딩을 거쳐, 최종적으로 JSTL(`<c:forEach>`)을 도입해 프레젠테이션 레이어에서 자바 코드를 분리해 내는 모던 웹 퍼블리싱의 진화 과정을 코드로 직접 체화함. 나아가 오라클 DB의 다중 테이블(EMP, DEPT) JOIN 결과를 JSP 화면에 동적으로 뿌려내는 백엔드 로직을 연동하고, CSS3의 위치 제어 속성(`relative`, `absolute`, `fixed`)과 `z-index`를 심층 학습하여 브라우저의 2차원 평면을 넘어선 3차원 공간 제어 및 레이어링 기술을 확보함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Experienced the limitations of rendering HTML using pure Java Servlets, leading to a structural understanding of the JSP rendering lifecycle (JSP &rarr; Servlet conversion &rarr; Compilation &rarr; HTML Response). Progressed from server-side data binding using scriptlets (`<% %>`) and expressions (`<%= %>`) to introducing JSTL (`<c:forEach>`), successfully separating Java logic from the presentation layer. Furthermore, integrated backend logic to dynamically render Oracle DB multi-table JOIN results (EMP, DEPT) onto JSP screens. Conducted an in-depth study of CSS3 positioning properties (`relative`, `absolute`, `fixed`) and `z-index`, securing the ability to control 3D space and element layering beyond the standard 2D document flow.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> 純粋なJavaコードでHTMLを描画するサーブレット（Servlet）方式の限界を実習し、これを克服するために登場したJSPの内部レンダリングライフサイクル（JSP &rarr; Servlet変換 &rarr; コンパイル &rarr; HTML応答）を構造的に完全に理解した。スクリプトレット（`<% %>`）と式（`<%= %>`）を用いたサーバーサイドのデータバインディングを経て、最終的にJSTL（`<c:forEach>`）を導入し、プレゼンテーションレイヤーからJavaコードを分離するモダンWebパブリッシングの進化過程をコードで直接体得。さらに、Oracle DBの複数テーブル（EMP、DEPT）JOIN結果をJSP画面に動的に表示するバックエンドロジックを連動させ、CSS3の位置制御属性（`relative`、`absolute`、`fixed`）と`z-index`を深化学習し、ブラウザの2次元平面を超えた3次元空間制御およびレイヤリング技術を確保した。</li></ul> |
| **05-19** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> JSP의 핵심 요소인 page 지시자의 다양한 속성(buffer, errorPage, contentType)을 제어하여 서버 메모리 최적화 및 안정적인 예외 처리 라우팅을 구현함. 정적 include 지시자를 활용해 반복되는 화면 요소를 분리하고 조립하는 조립식 레이아웃 아키텍처를 학습했으며, 브라우저 응답 포맷을 JSON/XML 포맷으로 변경하여 공공 데이터 Open API 공급 레이어의 기초를 정립함. 또한 request 내장 객체를 통해 클라이언트의 IP, URI 등의 서버 메타데이터를 추출하고, GET/POST 전송 방식에 따른 단일값 및 다중값(Hobby 배열)의 한글 깨짐 방지(setCharacterEncoding) 및 데이터 수신 메커니즘을 마스터함. 최종적으로 이를 싱글톤 패턴 기반의 GoodsDAO/VO 데이터 레이어와 결합하여, Flex 레이아웃 기반의 상품 목록, 블록 페이징, 그리고 상품 상세 보기 화면까지 유기적으로 이어지는 완성도 높은 웹 커머스 시스템의 데이터 파이프라인을 구축함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Mastered core JSP elements by controlling various attributes of the page directive (buffer, errorPage, contentType) to achieve server memory optimization and robust exception handling routing. Implemented a modular layout architecture by separating and assembling repeating UI components using the static include directive, and established the foundation for an Open API data delivery layer by alternating response formats into JSON and XML. Explored client-server data interaction using the request implicit object to extract server metadata (IP, URI) and handle single/multi-value form data across GET and POST methods with proper encoding alignment. Finally, integrated these concepts with a singleton-patterned GoodsDAO/VO data layer to construct a comprehensive web commerce system featuring flex-wrap product catalogs, block pagination, and interactive product detail views.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> JSPの中核要素であるpageディレクティブの多様な属性（buffer、errorPage、contentType）を制御し、サーバーメモリの最適化および安定的な例外処理ルーティングを実装。静的includeディレクティブを活用して、繰り返される画面要素を分離・結合する組込式レイアウトアーキテクチャを確立し、ブラウザへの応答フォーマットをJSON/XML形式に変更することでオープンAPIデータ提供の基礎を定立した。また、request内蔵オブジェクトを通じてクライアントのIPやURIなどのメタデータを抽出し、GET/POST送信方式に応じた単一値および複数値（配列）のデータ受信メカニズムをマスターした。最終的に、これらをシングルトンパターンベースのGoodsDAO/VOデータレイヤーと結合させ、Flexレイアウト基盤の商品一覧、ブロックページング、商品詳細画面へと有機的に繋がる完成度の高いウェブコマースシステムのデータパイプラインを構築した。</li></ul> |
| **05-20** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> JSP 내장 객체(`application`, `response`, `out`)의 생명주기를 활용해 HTTP 프로토콜을 정밀하게 제어하고, `web.xml`을 통한 글로벌 환경 설정 패턴을 구현함. 무상태(Stateless) 프로토콜을 보완하는 쿠키(Cookie) 제어 로직(`detail_before.jsp`)으로 최근 방문 기록 시스템의 기반을 설계함. 또한 HTTP 헤더 제어 및 이진 스트림 처리를 통한 파일 다운로드 기능을 구현하며 `out.clear()`를 이용한 버퍼 충돌 방어 코드를 체화함. 나아가 JSP 표준 액션 태그인 `<jsp:forward>`를 도입하여 웹의 핵심 제어 기술인 `response.sendRedirect()`와 `<jsp:forward>`의 동작 메커니즘을 비교 분석함. 클라이언트 측에서 주소를 새로 요청하여 `request` 객체를 초기화하는 리다이렉트 방식과, 서버 내부에서 `request` 객체와 전달된 파라미터 데이터를 유지한 채 화면을 전환하는 포워드 방식의 근본적인 차이를 이해하고 적재적소에 라우팅을 제어하는 웹 아키텍처 능력을 확립함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Mastered precise HTTP control using JSP implicit objects (`application`, `response`, `out`) and implemented global configuration patterns via `web.xml`. Designed cookie state management architecture (`detail_before.jsp`) to handle user tracking across stateless connections. Developed a secure file download layer handling binary I/O streams and resolved buffer collision exceptions using `out.clear()`. Furthermore, introduced standard JSP action tags (`<jsp:forward>`) to compare page routing mechanisms against `response.sendRedirect()`. Deeply analyzed the structural difference between client-side redirection, which resets the request scope, and server-side forwarding, which preserves data payloads and request attributes while maintaining the original URL.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> JSP内蔵オブジェクト（`application`、`response`、`out`）を活用してHTTPプロトコルを精密に制御し、`web.xml`を通じたグローバル環境設定パターンを実装。無状態（Stateless）プロトコルを補完するクッキー（Cookie）制御ロジック（`detail_before.jsp`）で最近の訪問履歴システムの基盤を設計した。また、HTTPヘッダー制御およびバイナリストリーム処理によるファイルダウンロード機能を実装し、`out.clear()`を用いたバッファ衝突防衛コードを体得。さらに、JSP標準アクションタグ（`<jsp:forward>`）を導入し、`response.sendRedirect()`と`<jsp:forward>`のルーティングメカニズムを比較分析した。クライアント側でリクエストを再送して`request`オブジェクトを初期化するリダイレクト方式と、サーバー内部でデータと属性を維持したまま画面を遷移させるフォワード方式の根本的な違いを理解し、Webアーキテクチャの基盤を確立した。</li></ul> |
| **05-21** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>JSP/Servlet 아키텍처 및 렌더링 심화 학습</strong>: 단순히 기능을 구현하는 단계를 넘어, 웹 서버가 JSP를 서블릿으로 변환하고 컴파일하여 HTML을 응답하는 생명주기(Lifecycle)를 추적함. `PrintWriter`를 이용한 원시적인 서블릿 응답 방식과 JSP의 렌더링 방식 차이를 이해하고, `directive`를 활용해 서버 자원(buffer, errorPage)을 통제하는 법을 학습함. <br><strong>상태 관리 및 데이터 파이프라인</strong>: Stateless한 HTTP 프로토콜의 한계를 극복하기 위해 `Cookie`와 `Session`을 활용해 사용자의 상태를 유지하는 기법을 체화함. 특히 서버 사이드 세션과 클라이언트 사이드 쿠키의 보안적·구조적 차이를 구분하고, `detail_before.jsp`와 같은 라우팅 로직을 통해 상태 유지 시스템을 설계함. <br><strong>UI 모듈화 및 자바빈즈 패턴</strong>: 외부 템플릿을 `header`, `footer`, `aside` 등 최소 단위의 컴포넌트로 분리하고 `<jsp:include>`로 조립하는 모듈화 패턴을 습득함. 또한, 파라미터를 일일이 수신하던 방식에서 벗어나 자바빈즈(JavaBeans)와 `<jsp:setProperty>`를 통해 VO에 자동으로 데이터를 바인딩하는 객체 지향적 데이터 처리 흐름을 학습함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>JSP/Servlet Architecture</strong>: Traced the lifecycle from JSP to Servlet compilation to understand the underlying rendering mechanism beyond simple implementation. Learned to control server resources using directives. <br><strong>State Management</strong>: Mastered `Cookie` and `Session` techniques to overcome HTTP statelessness, designing state-persistence systems via routing logic. <br><strong>UI Modularization & JavaBeans</strong>: Learned to decompose static templates into reusable UI components (`header`, `footer`, `aside`) using `<jsp:include>`. Adopted the JavaBeans pattern for automatic data binding into VO objects, shifting from manual parameter handling to object-oriented data processing.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>JSP/Servletアーキテクチャ</strong>: JSPがサーブレットへ変換・コンパイルされ、HTMLを応答するレンダリングライフサイクルを構造的に理解。ディレクティブを活用したサーバーリソース制御を習得。 <br><strong>状態管理</strong>: HTTPのStatelessな特性を克服するため、CookieとSessionを使い分ける技法を体得。ルーティングロジックを通じた状態維持システムを設計。 <br><strong>UIモジュール化 & JavaBeans</strong>: 静的テンプレートをコンポーネント（header, footer, aside）に分解し、`<jsp:include>`で再利用するモジュール化パターンを学習。JavaBeansパターンを用いたVOオブジェクトへの自動データバインディングにより、オブジェクト指向的なデータ処理フローを確立した。</li></ul> |
| **05-22** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[중앙 집중형 라우팅 실습]</strong> 여러 페이지로 분산되던 요청을 `main.jsp`라는 단일 진입점으로 모으고, `jspChange.java`를 통해 URL 파라미터(`mode`)에 따라 화면을 동적으로 조립하는 프론트 컨트롤러(Front-Controller) 패턴의 초기 형태를 구현해 봄. <br><strong>[파일 업로드 처리]</strong> 텍스트 기반 폼 전송의 한계를 확인하고, `enctype="multipart/form-data"` 규약을 적용함. Servlet 3.0의 `@MultipartConfig` 어노테이션과 `Part` 객체를 활용하여 이진(Binary) 데이터를 서버의 물리적 디스크에 저장하는 과정을 실습함. <br><strong>[MyBatis 맛보기]</strong> 기존 자바 DAO 코드 내부에 하드코딩되던 SQL 쿼리의 유지보수 한계를 인지함. 이를 개선하기 위해 MyBatis 프레임워크를 가볍게 도입하여, XML 매퍼(`databoard.xml`)를 통해 비즈니스 로직과 SQL을 분리하는 '관심사의 분리'를 경험함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Routing Practice]</strong> Practiced an early form of the Front-Controller pattern by routing scattered requests through a single entry point (`main.jsp`) and dynamically assembling views using `jspChange.java` based on URL parameters (`mode`). <br><strong>[File Upload Handling]</strong> Recognized the limitations of text-based form submissions and applied the `enctype="multipart/form-data"` protocol. Implemented binary data storage to the server's physical disk using Servlet 3.0's `@MultipartConfig` and `Part` objects. <br><strong>[MyBatis Preview]</strong> Acknowledged the maintenance limitations of hardcoded SQL queries within Java DAOs. Briefly introduced the MyBatis framework to experience the 'separation of concerns' by separating business logic from SQL via XML mappers (`databoard.xml`).</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[中央集中型ルーティング実習]</strong> 分散していたリクエストを`main.jsp`という単一の入り口に集め、`jspChange.java`を通じてURLパラメータ（`mode`）に応じて画面を動的に組み立てるフロントコントローラー（Front-Controller）パターンの初期形態を実装してみた。 <br><strong>[ファイルアップロード処理]</strong> テキストベースのフォーム送信の限界を確認し、`enctype="multipart/form-data"`規約を適用。Servlet 3.0の`@MultipartConfig`アノテーションと`Part`オブジェクトを活用し、バイナリデータをサーバーの物理ディスクに保存する過程を実習した。 <br><strong>[MyBatisプレビュー]</strong> 既存のJava DAOコード内にハードコーディングされていたSQLクエリの保守の限界を認知。これを改善するためMyBatisフレームワークを軽く導入し、XMLマッパー（`databoard.xml`）を通じてビジネスロジックとSQLを分離する「関心事の分離」を経験した。</li></ul> |
| **05-26** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[View 레이어 순수성 확보]</strong> 기존 JSP 화면에 혼재되어 있던 Java 스크립틀릿(`<% %>`)과 표현식(`<%= %>`)의 유지보수 한계를 체감하고, 이를 대체하기 위해 EL(Expression Language)과 JSTL(Java Standard Tag Library)을 도입함. 객체의 프로퍼티(Getter) 접근 및 데이터 스코프 제어를 EL로 자동화하고, Java의 제어문(if, for)을 XML 기반의 JSTL Core 태그(`<c:forEach>`, `<c:choose>`)로 전환하여 HTML과의 구조적 이질감을 해소함. <br><strong>[프레젠테이션 로직의 분리]</strong> 백엔드 자바 클래스에서 수행하던 데이터 가공(날짜/숫자 포맷팅, 문자열 자르기) 로직을 JSTL의 `fmt`와 `fn` 라이브러리로 위임함. 이를 통해 백엔드는 순수 데이터를 전달하고 뷰(View)가 출력 형태를 결정하는 명확한 관심사의 분리(Separation of Concerns)를 경험하며 MVC 패턴의 기초를 확립함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[View Layer Purification]</strong> Recognized the maintenance overhead of mixing Java scriptlets (`<% %>`) with HTML. Introduced Expression Language (EL) and JSTL to decouple Java logic from the View layer. Automated property access and scope control using EL, and replaced procedural Java control flows (if, for) with declarative XML-based JSTL Core tags (`<c:forEach>`, `<c:choose>`). <br><strong>[Separation of Presentation Logic]</strong> Shifted data manipulation responsibilities (date/number formatting, string operations) from backend Java classes to the View layer using JSTL `fmt` and `fn` libraries. Experienced the core philosophy of the MVC pattern by allowing the backend to serve raw data while the View determines the presentation format.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[Viewレイヤーの純粋性確保]</strong> 既存のJSP画面に混在していたJavaスクリプトレット（`<% %>`）の保守の限界を体感し、これを代替するためにEL（Expression Language）とJSTL（Java Standard Tag Library）を導入。オブジェクトのプロパティへのアクセスやスコープ制御をELで自動化し、Javaの制御文（if, for）をXMLベースのJSTL Coreタグ（`<c:forEach>`, `<c:choose>`）に転換してHTMLとの構造的な違和感を解消した。 <br><strong>[プレゼンテーションロジックの分離]</strong> バックエンドのJavaクラスで実行していたデータ加工（日付/数値のフォーマット、文字列の切り出し）ロジックをJSTLの`fmt`および`fn`ライブラリに委任。これにより、バックエンドは純粋なデータを提供し、Viewが出力形式を決定するという明確な関心事の分離（Separation of Concerns）を経験し、MVCパターンの基礎を確立した。</li></ul> |
| **05-27** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[비즈니스 로직의 캡슐화]</strong> 기존 JSP 내부에 혼재되어 있던 DB 연동 및 페이징 연산 로직을 `FoodModel.java` 클래스로 완전히 분리함. Model 클래스가 `HttpServletRequest` 객체를 매개변수로 받아 데이터를 가공한 뒤 `setAttribute`로 바인딩하는 구조를 통해, View 레이어의 부담을 줄이고 MVC 패턴으로 나아가기 위한 과도기적 MV 아키텍처를 구현함. <br><strong>[View 레이어의 독립]</strong> `foodList.jsp`와 `foodModelList.jsp`를 대조 실습하며, Java 스크립틀릿 대신 JSTL/EL을 활용해 프론트엔드 코드를 깔끔하게 유지하는 방법을 체화함. <br><strong>[서버 데이터와 외부 API 연동]</strong> `foodModelDetail.jsp`에서 서버를 통해 DB에서 가져온 주소 데이터(`${vo.address}`)를 카카오맵(Kakao Map) 자바스크립트 API의 Geocoder 객체에 동적으로 주입하여, 백엔드 데이터가 프론트엔드 스크립트로 매끄럽게 연결되는 파이프라인을 확인함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Encapsulation of Business Logic]</strong> Extracted DB interaction and pagination logic from JSP into a dedicated `FoodModel.java` class. Implemented an intermediate MV architecture where the Model receives the `HttpServletRequest`, processes data, and binds it via `setAttribute`, significantly reducing the View layer's responsibility. <br><strong>[Independence of the View Layer]</strong> Compared `foodList.jsp` with `foodModelList.jsp` to practice maintaining clean frontend code using JSTL/EL instead of Java scriptlets. <br><strong>[Server Data & External API Integration]</strong> In `foodModelDetail.jsp`, dynamically injected server-fetched address data (`${vo.address}`) into the Kakao Map JavaScript API's Geocoder object, demonstrating a seamless data pipeline from backend to frontend scripts.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[ビジネスロジックのカプセル化]</strong> 既存のJSP内に混在していたDB連携およびページング演算ロジックを`FoodModel.java`クラスに完全に分離。Modelクラスが`HttpServletRequest`を引数として受け取り、データを加工して`setAttribute`でバインディングする構造により、Viewレイヤーの負担を軽減し、MVCパターンへ移行するための過渡期的なMVアーキテクチャを実装した。 <br><strong>[Viewレイヤーの独立]</strong> `foodList.jsp`と`foodModelList.jsp`を対照実習し、Javaスクリプトレットの代わりにJSTL/ELを活用してフロントエンドコードをクリーンに保つ方法を体得。 <br><strong>[サーバーデータと外部APIの連携]</strong> `foodModelDetail.jsp`において、DBから取得した住所データ（`${vo.address}`）をKakao Map JavaScript APIのGeocoderオブジェクトに動的に注入し、バックエンドデータがフロントエンドスクリプトへスムーズに連携されるパイプラインを確認した。</li></ul> |
| **05-28** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[계층형 답변 알고리즘 구현]</strong> 부모 글의 `group_id`, `group_step`, `group_tab`을 참조하여 답변 글의 순서와 깊이를 지정하는 로직을 구현함. 특히 중간에 답변이 끼어들 때 기존 답변들의 출력 순서(`group_step`)를 일괄 조정하는 `UPDATE` 쿼리와 새로운 답변을 `INSERT` 하는 복합적인 DB 처리 과정을 학습함. <br><strong>[비동기 통신(Ajax) 도입]</strong> 게시글 수정(`update.jsp`) 시 기존의 동기식 폼(Form) 전송 대신 jQuery의 `$.ajax`를 도입함. 화면 전체를 새로고침하지 않고 클라이언트에서 서버로 데이터를 전송한 뒤, `BoardModel`로부터 비밀번호 일치 여부(`yes`/`no`)만 텍스트 스트림으로 응답받아 동적으로 화면 이동 및 경고창을 제어하는 SPA(Single Page Application) 통신의 기초를 경험함. <br><strong>[MVC 패턴의 응답 분리]</strong> View(JSP)와 Controller(Model) 간의 역할 분리를 명확히 하여, HTML을 렌더링하는 응답과 순수 데이터(Text)만 반환하는 API 형태의 응답을 구분하여 설계함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Hierarchical Reply Algorithm]</strong> Implemented the core logic for reply boards by referencing the parent post's `group_id`, `group_step`, and `group_tab`. Learned to manage complex DB operations involving `UPDATE` queries to adjust existing reply sequences and `INSERT` queries for new replies. <br><strong>[Asynchronous Communication (Ajax)]</strong> Replaced synchronous form submissions with jQuery `$.ajax` for post updates (`update.jsp`). Experienced the basics of SPA communication by sending data to the server without page reloads and handling plain text responses (`yes`/`no`) from the `BoardModel` for password validation and dynamic routing. <br><strong>[MVC Response Separation]</strong> Clarified the separation of concerns between View and Controller, designing responses that distinguish between rendering HTML and returning pure data (API-like text streams).</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[階層型返信アルゴリズムの実装]</strong> 親記事の`group_id`、`group_step`、`group_tab`を参照し、返信記事の順序と深さを指定するロジックを実装。特に中間に返信が挿入される際、既存の返信の出力順序（`group_step`）を一括調整する`UPDATE`クエリと新規返信の`INSERT`を行う複合的なDB処理プロセスを学習した。 <br><strong>[非同期通信（Ajax）の導入]</strong> 記事修正（`update.jsp`）時に従来の同期式フォーム送信の代わりにjQueryの`$.ajax`を導入。画面全体をリロードせずにサーバーへデータを送信し、`BoardModel`からパスワードの一致結果（`yes`/`no`）のみをテキストストリームで受け取り、動的に画面遷移や警告を制御するSPA通信の基礎を経験した。 <br><strong>[MVCパターンの応答分離]</strong> View（JSP）とController（Model）間の役割分離を明確にし、HTMLをレンダリングする応答と純粋なデータ（Text）のみを返すAPI形式の応答を区別して設計した。</li></ul> |
| **05-29** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[Front-Controller 아키텍처 도입]</strong> 모든 클라이언트의 요청(`*.do`)을 단일 진입점인 `Controller` 서블릿으로 모아 중앙 통제하는 라우팅 아키텍처를 구현함. 공통 인터페이스(`Model.java`)를 활용한 다형성(Polymorphism)과 Command 패턴을 적용하여, 컨트롤러가 개별 비즈니스 로직의 구현체를 몰라도 유연하게 실행할 수 있는 구조를 설계함. <br><strong>[IoC 컨테이너 및 Reflection 체화]</strong> 코드 내부에 하드코딩된 객체 생성(`new`) 방식의 결합도 문제를 인지함. 이를 해결하기 위해 XML(`application.xml`) 파싱과 Java Reflection(`Class.forName()`)을 융합하여, 런타임에 동적으로 객체를 생성하고 Map에 주입하는 Spring Framework의 의존성 주입(DI) 원리를 순수 자바로 실습함. <br><strong>[동적 템플릿 레이아웃]</strong> `main.jsp`를 전체 UI의 뼈대로 설정하고, Model에서 분기 처리한 뷰 경로를 `${main_jsp}` 변수로 받아 동적으로 화면을 스위칭하는 효율적인 프레젠테이션 레이어 패턴을 적용함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Front-Controller Architecture]</strong> Implemented a central routing architecture by funneling all client requests (`*.do`) through a single entry point (`Controller` Servlet). Applied polymorphism and the Command pattern via a common interface (`Model.java`), allowing the controller to execute business logic flexibly without hardcoded dependencies. <br><strong>[IoC Container & Reflection]</strong> Addressed tight coupling issues caused by hardcoded object instantiation (`new`). Implemented the core philosophy of Spring's Dependency Injection by parsing XML configurations and utilizing Java Reflection (`Class.forName()`) to dynamically instantiate objects at runtime. <br><strong>[Dynamic Template Layout]</strong> Established `main.jsp` as the structural UI frame, using EL (`${main_jsp}`) to dynamically inject and switch content views based on paths determined by the Model layer.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[Front-Controller アーキテクチャの導入]</strong> 全てのクライアント要求（`*.do`）を単一の入り口である`Controller`サーブレットに集約し、中央制御するルーティングアーキテクチャを実装。共通インターフェース（`Model.java`）を活用した多態性（Polymorphism）とCommandパターンを適用し、コントローラーが個別のビジネスロジックを知らなくても柔軟に実行できる構造を設計した。 <br><strong>[IoCコンテナとReflectionの体得]</strong> コード内にハードコーディングされたオブジェクト生成（`new`）による結合度の問題を認知。これを解決するため、XMLパースとJava Reflection（`Class.forName()`）を融合させ、ランタイムに動的にオブジェクトを生成しMapに注入するSpring Frameworkの依存性注入（DI）の原理を純粋なJavaで実習した。 <br><strong>[動的テンプレートレイアウト]</strong> `main.jsp`をUI全体の骨組みとし、Modelで決定されたビューのパスを`${main_jsp}`変数で受け取り、動的に画面を切り替える効率的なプレゼンテーションレイヤーパターンを適用した。</li></ul> |
| **06-01** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[View 레이어의 순수성 확보]</strong> `boardList`, `boardDetail` 등 모든 JSP 화면에서 자바 스크립틀릿(`<% %>`)을 완전히 배제하고, JSTL과 EL만을 활용해 컨트롤러가 넘겨준 데이터를 렌더링하는 진정한 의미의 MVC View 구성을 완료함. <br><strong>[파일 I/O 스트림 및 다운로드 구현]</strong> `download.jsp`를 통해 브라우저의 기본 텍스트 렌더링을 차단하고(`Content-Disposition`), `application.getRealPath()`로 서버의 물리적 경로를 추적함. `BufferedInputStream`과 `OutputStream`을 연결하여 바이너리 데이터를 클라이언트 로컬 디스크로 안전하게 밀어내는 백엔드 스트림 제어를 실습함. <br><strong>[jQuery 기반의 동적 UI 토글]</strong> `boardDetail.jsp`에서 삭제 버튼 클릭 시 화면 전환 없이 jQuery의 `show()`, `hide()`를 이용해 숨겨진 폼(`<tr style="display:none">`)을 동적으로 제어하는 기초적인 프론트엔드 상호작용을 경험함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[View Layer Purification]</strong> Completely eliminated Java scriptlets (`<% %>`) from all JSP views (`boardList`, `boardDetail`), utilizing only JSTL and EL to render data passed by the controller, finalizing a strict MVC View architecture. <br><strong>[File I/O Stream & Download]</strong> Implemented `download.jsp` to bypass default browser text rendering using the `Content-Disposition` header. Tracked physical server paths via `application.getRealPath()` and utilized `BufferedInputStream/OutputStream` to securely pipe binary data to the client's local disk. <br><strong>[jQuery-based UI Toggling]</strong> Experienced basic frontend interaction in `boardDetail.jsp` by using jQuery `show()` and `hide()` to dynamically toggle hidden form rows without requiring a page reload.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[Viewレイヤーの純粋性確保]</strong> `boardList`や`boardDetail`など全てのJSP画面からJavaスクリプトレット（`<% %>`）を完全に排除し、JSTLとELのみを活用してコントローラーから渡されたデータをレンダリングする、真の意味でのMVC View構成を完了した。 <br><strong>[ファイルI/Oストリームとダウンロード実装]</strong> `download.jsp`を通じてブラウザのデフォルトテキストレンダリングを遮断し（`Content-Disposition`）、`application.getRealPath()`でサーバーの物理パスを追跡。`BufferedInputStream`と`OutputStream`を接続し、バイナリデータをクライアントのローカルディスクへ安全に転送するバックエンドのストリーム制御を実習した。 <br><strong>[jQueryベースの動的UI制御]</strong> `boardDetail.jsp`にて、削除ボタンのクリック時に画面遷移なしでjQueryの`show()`、`hide()`を用いて隠しフォーム（`<tr style="display:none">`）を動的に制御する基礎的なフロントエンドインタラクションを経験した。</li></ul> |
| **06-02** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[어노테이션 기반 라우팅 및 Reflection]</strong> 기존 `Map` 기반의 Command 패턴을 폐기하고, 사용자 정의 어노테이션(`@Controller`, `@RequestMapping`)과 리플렉션을 활용해 런타임에 동적으로 URL과 메서드를 매핑하는 구조를 도입함. 공통 인터페이스(`Model.java`)의 제약 없이 하나의 클래스 내에 여러 매핑 메서드를 배치하는 POJO 기반의 모던 MVC 패턴을 구현함. <br><strong>[MyBatis 모듈화 및 설정 외부화]</strong> `food-mapper.xml`에서 반복되는 쿼리를 `<sql>` 태그로 분리하여 `<include>`로 재사용하는 기법을 실습함. 또한 `Config.xml`에 하드코딩되던 DB 접속 정보를 `db.properties`로 분리하여 보안을 강화하고 배포 환경 변화에 유연하게 대응할 수 있는 구조를 마련함. <br><strong>[동적 템플릿 레이아웃 고도화]</strong> `header.jsp`와 `main.jsp`를 분리 연동하여, 컨트롤러의 요청(`*.do`)에 따라 `main.jsp`의 본문 영역(`${main_jsp}`)만 동적으로 렌더링되도록 처리함. 전체 페이지 갱신 없이 UI 컴포넌트를 조립하는 레이아웃 패턴을 안착시킴.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Annotation-based Routing & Reflection]</strong> Replaced the Map-based Command pattern with custom annotations (`@Controller`, `@RequestMapping`) and Reflection for dynamic URL-to-method mapping at runtime. Implemented a POJO-based modern MVC pattern allowing multiple mapping methods per class without interface constraints. <br><strong>[MyBatis Modularization & External Config]</strong> Modularized repetitive queries in `food-mapper.xml` using `<sql>` and `<include>` tags. Extracted hardcoded DB credentials from `Config.xml` into `db.properties` to enhance security and deployment flexibility. <br><strong>[Refined Dynamic Layout]</strong> Modularized UI into `header.jsp` and `main.jsp`. Implemented a dynamic layout pattern where only the main content area (`${main_jsp}`) is re-rendered based on routing, establishing a component-based UI architecture.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[アノテーションベースのルーティングとReflection]</strong> 従来のMapベースのCommandパターンを廃棄し、独自アノテーション（`@Controller`、`@RequestMapping`）とリフレクションを活用して動的にURLとメソッドをマッピングする構造を導入。共通インターフェースの制約なしに1つのクラス内に複数のマッピングメソッドを配置できるPOJOベースのモダンMVCパターンを実装した。<br><strong>[MyBatisのモジュール化と設定の外部化]</strong> `food-mapper.xml`内の反復クエリを`<sql>`タグで分離し`<include>`で再利用する技法を実習。また、`Config.xml`にハードコーディングされていたDB接続情報を`db.properties`に分離し、セキュリティの強化とデプロイ環境への柔軟な対応を可能にした。<br><strong>[動的テンプレートレイアウトの高度化]</strong> `header.jsp`と`main.jsp`をモジュール化し、ルーティングに応じて本文領域（`${main_jsp}`）のみを動的にレンダリングするコンポーネントベースのUIレイアウトパターンを定着させた。</li></ul> |
| **06-04** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[외부 API 연동 (Kakao Map)]</strong> `foodDetail.jsp`에서 서버가 DB로부터 조회하여 넘겨준 텍스트 주소 데이터(`${vo.address}`)를 카카오맵(Kakao Map) JavaScript API의 `Geocoder` 객체에 동적으로 주입함. 백엔드 데이터와 프론트엔드 외부 라이브러리 간의 파이프라인을 구축하여 지도 위 좌표로 변환(Geocoding) 및 마커 렌더링 로직을 구현함. <br><strong>[동적 템플릿 레이아웃 정립]</strong> `index.jsp` 진입 시 즉시 `main.do` 컨트롤러로 리다이렉트 시키고, `main.jsp`를 뼈대로 삼아 `<jsp:include page="${main_jsp }">`를 통해 본문 내용만 동적으로 교체하는 UI 컴포넌트화 패턴을 안착시킴. <br><strong>[View 계층의 순수성 유지]</strong> `home.jsp`의 페이징 처리 및 썸네일 그리드 출력 과정에서 자바 스크립틀릿(`<% %>`)을 배제하고 JSTL(`<c:forEach>`, `<c:if>`)과 EL만을 활용하여 프레젠테이션 로직의 가독성과 독립성을 확보함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[External API Integration]</strong> In `foodDetail.jsp`, dynamically injected server-fetched address data (`${vo.address}`) into the Kakao Map JavaScript API's `Geocoder`. Established a data pipeline between the backend and a frontend external library to perform geocoding and render map markers. <br><strong>[Dynamic Template Layout]</strong> Configured `index.jsp` to redirect immediately to the `main.do` controller, establishing `main.jsp` as the structural template. Utilized `<jsp:include page="${main_jsp }">` to dynamically swap content components. <br><strong>[View Layer Purification]</strong> Eliminated Java scriptlets from `home.jsp` during pagination and thumbnail grid rendering, exclusively relying on JSTL and EL to maintain the presentation layer's independence and readability.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[外部API連携 (Kakao Map)]</strong> `foodDetail.jsp`において、サーバーがDBから取得した住所データ（`${vo.address}`）をKakao Map JavaScript APIの`Geocoder`オブジェクトに動的に注入。バックエンドデータとフロントエンド外部ライブラリ間のデータパイプラインを構築し、地図上の座標への変換（Geocoding）およびマーカーのレンダリングロジックを実装した。<br><strong>[動的テンプレートレイアウトの定着]</strong> `index.jsp`アクセス時に直ちに`main.do`コントローラーへリダイレクトさせ、`main.jsp`を骨組みとして`<jsp:include page="${main_jsp }">`を通じて本文内容のみを動的に入れ替えるUIコンポーネント化パターンを定着させた。<br><strong>[View階層の純粋性維持]</strong> `home.jsp`のページング処理やサムネイルグリッド出力過程からJavaスクリプトレット（`<% %>`）を排除し、JSTLとELのみを活用することでプレゼンテーションロジックの可読性と独立性を確保した。</li></ul> |
| **06-05** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[JavaScript 기초 문법]</strong> 기존 자바 백엔드 로직에서 벗어나, 브라우저 단에서 동작하는 JS 기초를 학습함. `var`의 호이스팅 한계를 극복하는 `let`과 `const`의 블록 스코프 개념을 이해하고, 자바스크립트 특유의 동치 연산자(`===`), 타입 형변환(`Number()`), 예외 데이터(`NaN`, `Infinity`) 처리 방식을 실습함. <br><strong>[배열과 객체 순회]</strong> 전통적인 `for`문을 넘어, 컬렉션 처리에 최적화된 모던 JS 순회 문법(`for...in`, `for...of`, `forEach`, `map`)을 적용하여 배열 데이터를 화면에 출력하는 과정을 코드로 체화함. <br><strong>[Linux 인프라 구축 및 배포]</strong> 리눅스 기본 명령어와 환경 설정에 익숙해지기 위해 기존 Ubuntu OS를 완전히 초기화(포맷)하고 재설치함. 이후 순수 환경에서 Tomcat WAS를 직접 세팅하고 작성한 웹 애플리케이션을 배포(Deploy)하는 일련의 서버 운영 사이클을 점검함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[JavaScript Fundamentals]</strong> Transitioned to frontend scripting, studying vanilla JS. Grasped block-scoping with `let` and `const`, JS-specific strict equality (`===`), type conversions, and handling anomalies like `NaN` and `Infinity`. <br><strong>[Array/Object Iteration]</strong> Moved beyond traditional loops to implement modern JS iteration methods (`for...in`, `for...of`, `forEach`, `map`) for rendering collection data. <br><strong>[Linux Infrastructure & Deployment]</strong> Wiped and reinstalled the Ubuntu OS to practice basic Linux commands and system configuration. Manually provisioned a Tomcat server from scratch and successfully performed deployment tests, solidifying understanding of the server operations cycle.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[JavaScript 基礎文法]</strong> ブラウザ上で動作するJSの基礎を学習。`let`と`const`によるブロックスコープの概念を理解し、JS特有の厳密等価演算子（`===`）、型変換、例外データ（`NaN`、`Infinity`）の処理方式を実習した。<br><strong>[配列とオブジェクトの反復処理]</strong> 従来の`for`文を超え、コレクション処理に最適化されたモダンJSの反復構文（`for...in`、`for...of`、`forEach`、`map`）を適用し、配列データを画面に出力するプロセスをコードで体得した。<br><strong>[Linux インフラ構築とデプロイ]</strong> Linuxの基本コマンドと環境設定に慣れるため、既存のUbuntu OSを完全に初期化（フォーマット）して再インストール。その後、クリーンな環境でTomcat WASを手動でセットアップし、Webアプリケーションのデプロイテストを行う一連のサーバー運用サイクルを確認した。</li></ul> |
