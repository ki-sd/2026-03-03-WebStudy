# <img src="https://flagcdn.com/w20/kr.png" width="20" style="vertical-align: middle;"> JAVA AWS CI/CD 풀스택과정 - Web<br>
### <img src="https://flagcdn.com/w20/us.png" width="20" style="vertical-align: middle;"> JAVA AWS CI/CD Full-stack Course - Web<br>
### <img src="https://flagcdn.com/w20/jp.png" width="20" style="vertical-align: middle;"> JAVA AWS CI/CD フルスタック課程 - Web<br>

<br>
<br>

* **Progress** : <img src="https://img.shields.io/badge/Progress-60%25-yellow?style=flat-square" align="absmiddle">
* **Language & Markup** : <img src="https://img.shields.io/badge/Java_21-ED8B00?style=flat-square&logo=openjdk&logoColor=white" align="absmiddle"> <img src="https://img.shields.io/badge/HTML5-E34F26?style=flat-square&logo=html5&logoColor=white" align="absmiddle"> <img src="https://img.shields.io/badge/CSS3-1572B6?style=flat-square&logo=css3&logoColor=white" align="absmiddle">
* **Framework & Library** : <img src="https://img.shields.io/badge/JSP-007396?style=flat-square&logo=java&logoColor=white" align="absmiddle"> <img src="https://img.shields.io/badge/Vue.js-4FC08D?style=flat-square&logo=vuedotjs&logoColor=white" align="absmiddle"> <img src="https://img.shields.io/badge/jQuery-0769AD?style=flat-square&logo=jquery&logoColor=white" align="absmiddle">
* **Network & API** : <img src="https://img.shields.io/badge/Axios-5A29E4?style=flat-square&logo=axios&logoColor=white" align="absmiddle">
* **DataBase** : <img src="https://img.shields.io/badge/Oracle_21c-F80000?style=flat-square&logo=oracle&logoColor=white" align="absmiddle">
* **Server** : <img src="https://img.shields.io/badge/Apache_Tomcat-F8DC75?style=flat-square&logo=apachetomcat&logoColor=black" align="absmiddle">
* **IDE & Tools** : <img src="https://img.shields.io/badge/Eclipse-2C2255?style=flat-square&logo=eclipseide&logoColor=white" align="absmiddle"> <img src="https://img.shields.io/badge/VS_Code-007ACC?style=flat-square&logo=visualstudiocode&logoColor=white" align="absmiddle"> <img src="https://img.shields.io/badge/SQL_Developer-F80000?style=flat-square&logo=oracle&logoColor=white" align="absmiddle"> <img src="https://img.shields.io/badge/DBeaver-382923?style=flat-square&logo=dbeaver&logoColor=white" align="absmiddle">

<br>
<br>

📅 학습 일지 (Study Log / 学習記録)
------------------------------

<br>
자세한 내용: <a href="https://ki-sd.tistory.com/category/WebStudy" target="_blank"><img src="https://img.shields.io/badge/Tistory-000000?style=flat-square&logo=tistory&logoColor=white&link=https://ki-sd.tistory.com/"></a>
<br>

#### 2026-04
| 날짜 | 학습 내용 (KR / US / JP) |
| :--- | :--- |
| **05-06** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> 데스크톱 환경(Swing)을 벗어나 본격적인 웹 개발(JSP)에 입문함. Bootstrap 프레임워크를 활용하여 브라우저 UI를 구성하고, 오라클 DB와 연동된 맛집(Food) 데이터를 스크립틀릿(`<% %>`)과 표현식(`<%= %>`)을 통해 HTML 태그에 동적으로 렌더링함. `request.getParameter()`를 이용해 URL 쿼리스트링을 제어하고 삼항 연산자를 활용한 웹 기반 페이징 로직을 완벽하게 구현함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Initialized the WebStudy repository, transitioning from desktop (Swing) to Web (JSP) development. Rendered dynamic food data from Oracle DB onto HTML using scriptlets (`<% %>`) and expressions (`<%= %>`), styling the UI with Bootstrap. Mastered web-based pagination logic by controlling URL query strings via `request.getParameter()` and applying ternary operators.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> デスクトップ環境（Swing）から本格的なWeb開発（JSP）へ入門。Bootstrapフレームワークを活用してブラウザのUIを構成し、Oracle DBと連携した飲食店（Food）データをスクリプトレット（`<% %>`）と式（`<%= %>`）を用いてHTMLタグに動的にレンダリング。`request.getParameter()`を利用したURLクエリ文字列の制御と三項演算子を活用し、Webベースのページングロジックを実装した。</li></ul> |
| **05-07** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> 웹의 동작 원리(Client-Server 구조, HTTP 상태 코드)와 웹 표준(HTML5, CSS3, ES6)의 핵심 개념을 정립함. 프론트엔드와 백엔드 언어들의 역할을 구분하고, HTML5 문서의 기본 뼈대(`<!DOCTYPE html>`, `<head>`, `<body>`) 구조를 분석함. 또한 `<h1>`~`<h6>`, `<p>`, `<br>`, `<pre>` 등의 단락 제어 태그와 `<strong>`, `<mark>`, `<sup>` 등 다양한 문자 제어 태그를 실습하며 정적 웹 UI 렌더링의 기초를 다짐.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Established the core concepts of web operation principles (Client-Server architecture, HTTP status codes) and web standards (HTML5, CSS3, ES6). Distinguished the roles of frontend and backend languages and analyzed the basic structure of an HTML5 document. Practiced paragraph control tags (`<h1>`-`<h6>`, `<p>`, `<br>`, `<pre>`) and character formatting tags (`<strong>`, `<mark>`, `<sup>`) to build a solid foundation in static web UI rendering.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> Webの動作原理（クライアント・サーバー構造、HTTPステータスコード）とWeb標準（HTML5、CSS3、ES6）の核心概念を確立。フロントエンドとバックエンド言語の役割を区別し、HTML5ドキュメントの基本構造を分析した。また、段落制御タグ（`<h1>`〜`<h6>`、`<p>`、`<br>`、`<pre>`）や多様な文字制御タグ（`<strong>`、`<mark>`、`<sup>`など）を実習し、静的Web UIレンダリングの基礎を固めた。</li></ul> |
| **05-08** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> HTML5의 핵심인 테이블(`colspan`/`rowspan`)과 목록 태그를 활용한 시맨틱 UI 설계부터, 사용자 입력을 처리하는 폼(Form) 컨트롤(`<input>`, `<select>`, `<fieldset>`)까지 총망라하여 학습함. 정적 웹의 한계를 넘어 JQuery를 도입해 사원 목록을 동적으로 필터링(`keyup`)하는 프론트엔드 제어 기법을 익히고, 카카오맵 Open API를 연동해 문자열 주소를 지도 마커로 변환하는 외부 데이터 통신 기법을 실습함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Comprehensively studied HTML5 by designing semantic UIs using tables (`colspan`/`rowspan`) and lists, and mastering form controls (`<input>`, `<select>`) for user input. Overcame static web limitations by introducing JQuery for dynamic frontend filtering (`keyup`) of employee lists. Additionally, integrated the Kakao Map Open API to convert string addresses into interactive map markers, practicing practical external data communication.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> HTML5の核心であるテーブル（`colspan`/`rowspan`）やリストタグを活用したセマンティックUI設計から、ユーザー入力を処理するフォーム（Form）コントロール（`<input>`、`<select>`）まで網羅的に学習。静的Webの限界を超え、JQueryを導入して社員一覧を動的にフィルタリング（`keyup`）するフロントエンド制御技法を習得し、カカオマップOpen APIを連動して文字列の住所を地図マーカーに変換する外部データ通信技法を実習した。</li></ul> |
| **05-11** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> HTML5의 시맨틱 마크업(표, 목록, 폼, 텍스트 제어)과 CSS3의 스타일링 기법을 결합하여 프론트엔드 UI/UX 설계의 기반을 완성함. 전체, 태그, 클래스(`.`), 아이디(`#`) 선택자의 우선순위를 이해하고, 속성 선택자(`[attr^=value]`)와 계층 선택자(자손 `>`, 후손, 동위 `+`, `~`)를 활용해 원하는 DOM 요소를 정교하게 타겟팅하는 기법을 마스터함. 또한 `:hover` 반응 선택자를 이용하여 테이블 행이나 이미지에 동적인 시각적 피드백을 부여하는 실무 퍼블리싱 스킬을 체화함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Completed the foundation of frontend UI/UX design by combining HTML5 semantic markup (tables, lists, forms, text formatting) with CSS3 styling techniques. Mastered targeting DOM elements precisely using universal, tag, class (`.`), and ID (`#`) selectors based on specificity rules, along with attribute selectors (`[attr^=value]`) and combinators (child `>`, descendant, sibling `+`, `~`). Applied `:hover` pseudo-classes to images and table rows to provide dynamic visual feedback, acquiring practical web publishing skills.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> HTML5のセマンティックマークアップ（テーブル、リスト、フォーム、テキスト制御）とCSS3のスタイリング技法を結合し、フロントエンドUI/UX設計の基盤を完成。全体、タグ、クラス（`.`）、ID（`#`）セレクタの優先順位を理解し、属性セレクタ（`[attr^=value]`）や階層セレクタ（子 `>`, 孫、兄弟 `+`, `~`）を活用して目的のDOM要素を精巧にターゲティングする技法をマスターした。また、`:hover` 疑似クラスを用いてテーブルの行や画像に動的な視覚的フィードバックを付与する実務パブリッシングスキルを体得した。</li></ul> |
| **05-12** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> CSS 박스 모델(margin, padding, border)과 고급 선택자(구조, 상태, 속성)를 총망라하여 반응형 프론트엔드 스타일링의 기초를 다짐. 나아가 순수 HTML의 정적 한계를 극복하기 위해 JSP(Java Server Pages)를 도입함. `empDAO`와 `empVO`를 통해 오라클 DB의 다중 테이블(EMP, DEPT) JOIN 쿼리 결과를 가져오고, 스크립틀릿(`<% %>`)과 표현식(`<%= %>`)을 사용해 브라우저 화면(HTML Table)에 동적으로 렌더링하는 풀스택 웹 아키텍처를 구현함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Mastered frontend styling basics by comprehensively covering the CSS Box Model (margin, padding, border) and advanced selectors (structural, pseudo-classes, attribute). Overcame the static limitations of HTML by introducing JSP (Java Server Pages). Implemented a full-stack architecture that fetches Oracle DB JOIN query results (EMP, DEPT) via DAO/VO patterns and dynamically renders them onto the browser using scriptlets (`<% %>`) and expressions (`<%= %>`).</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> CSSボックスモデル（margin、padding、border）および高度なセレクタ（構造、状態、属性）を網羅し、フロントエンドのスタイリングの基礎を確立。さらに純粋なHTMLの静的限界を克服するためJSP（Java Server Pages）を導入した。DAO/VOパターンを通じてOracle DBの複数テーブル（EMP、DEPT）JOINクエリ結果を取得し、スクリプトレット（`<% %>`）と式（`<%= %>`）を使用してWebブラウザの画面に動的レンダリングするフルスタックWebアーキテクチャを実装した。</li></ul> |
| **05-13** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> CSS 박스 모델(Margin, Padding, Border)의 시계 방향 단축 속기법과 가시성(`display`), 배경(`background`) 제어 속성을 심층적으로 마스터함. 순수 HTML/CSS의 정적인 한계를 JQuery의 상태 토글(`hide()`, `show()`)로 극복하여 아코디언(더보기) UI를 구현함. 백엔드 측면에서는 Model 1 아키텍처 기반의 맛집 포털 프로젝트를 고도화하여, Oracle DB의 `OFFSET FETCH`를 이용한 서버 사이드 페이징 로직과 Bootstrap 그리드 시스템을 결합함. 특히 카카오맵 Geocoder API를 도입해 DB에 저장된 텍스트 주소를 실시간 위경도 좌표로 변환하여 렌더링하는 클라이언트-서버-외부 API 간의 완벽한 3단 통신 시스템을 구축함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Mastered CSS Box Model shorthands (Margin, Padding, Border) and deeply explored visibility (`display`) and `background` properties. Overcame static limitations by implementing an accordion (Read More) UI using JQuery's state toggles (`hide()`, `show()`). On the backend, advanced a Food Portal project based on the Model 1 architecture, integrating server-side pagination (`OFFSET FETCH`) with the Bootstrap grid system. Notably, built a robust 3-tier communication system (Client-Server-External API) by integrating the Kakao Map Geocoder API to convert stored text addresses into real-time interactive map coordinates.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> CSSボックスモデルの短縮記法（Margin、Padding、Border）と可視性（`display`）、背景（`background`）制御属性を深層的にマスター。純粋なHTML/CSSの静的限界をJQueryの状態トグル（`hide()`、`show()`）で克服し、アコーディオン（もっと見る）UIを実装した。バックエンドの側面では、Model 1アーキテクチャベースの飲食店ポータルプロジェクトを高度化し、Oracle DBの`OFFSET FETCH`を利用したサーバーサイドページングロジックとBootstrapグリッドシステムを結合。特にカカオマップGeocoder APIを導入し、DBに保存されたテキスト住所をリアルタイムの緯度経度座標に変換してレンダリングする、クライアント・サーバー・外部API間の完璧な3層通信システムを構築した。</li></ul> |
| **05-14** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> CSS3의 가시성 속성(`display`, `visibility`, `opacity`)을 활용해 문서 객체 모델(DOM) 상의 공간 점유와 이벤트 수신 여부를 제어함. `text-overflow: ellipsis`와 `white-space`를 결합하여 맛집 및 음악 목록의 긴 텍스트를 깔끔하게 처리하는 실무 퍼블리싱 기법을 체화함. 백엔드 측면에서는 기존 JSP Model 1 구조를 확장하여 외부 API(KOBIS) 연동을 위한 '프록시(Proxy) 서버 패턴'을 도입함. Java(`MovieManager`)로 원격 데이터를 수집하고 JSP(`result.jsp`)가 중계하여 프론트엔드로 전달하는 파이프라인을 구축하며, 정적 화면을 넘어선 데이터 기반의 동적 웹 애플리케이션 아키텍처를 확립함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Mastered precise control over DOM spatial allocation and event reception using CSS3 visibility properties (`display`, `visibility`, `opacity`). Internalized practical publishing techniques by combining `text-overflow: ellipsis` and `white-space` to handle long text in Food/Music lists. On the backend, expanded the existing JSP Model 1 structure by introducing a 'Proxy Server Pattern' for external API (KOBIS) integration. Established a dynamic, data-driven web application architecture by building a pipeline where Java (`MovieManager`) collects remote data and JSP (`result.jsp`) relays it to the frontend.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> CSS3の可視性属性（`display`、`visibility`、`opacity`）を活用し、DOM上の空間占有とイベント受信を精密に制御。`text-overflow: ellipsis`と`white-space`を組み合わせ、飲食店や音楽一覧の長いテキストをきれいに処理する実務パブリッシング技法を体得した。バックエンドの側面では、既存のJSP Model 1構造を拡張し、外部API（KOBIS）連携のための「プロキシサーバーパターン」を導入。Java（`MovieManager`）でリモートデータを収集し、JSP（`result.jsp`）が中継してフロントエンドに伝達するパイプラインを構築し、静的画面を超えたデータ駆動型の動的Webアプリケーションアーキテクチャを確立した。</li></ul> |
| **05-15** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> JSP Model 1 아키텍처를 기반으로 웹 서비스의 핵심인 게시판 CRUD(생성, 조회, 수정, 삭제) 전체 사이클을 완벽하게 구현함. 데이터 입력 폼(View)과 DB 처리 전용 페이지(`_ok.jsp`)를 분리하여 초기 형태의 컨트롤러 패턴을 설계함. `SimpleDateFormat`을 활용해 당일 작성된 글에만 'NEW' 뱃지를 띄우는 동적 UI 로직과, JS `scrollHeight`를 이용한 텍스트에어리어 자동 크기 조절 등 실무적인 UX 디테일을 추가함. 또한 트랜잭션 성공 여부(`boolean`)에 따라 `sendRedirect`와 JS `history.back()`을 분기 처리하는 백엔드 라우팅 제어 기법을 확립함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Completely implemented the full CRUD (Create, Read, Update, Delete) cycle of a web bulletin board based on the JSP Model 1 architecture. Designed an early form of the controller pattern by separating data input forms (View) from DB processing pages (`_ok.jsp`). Added practical UX details, such as dynamically rendering a 'NEW' badge for posts created today using `SimpleDateFormat`, and auto-resizing textareas using JS `scrollHeight`. Established backend routing control techniques by branching `sendRedirect` and JS `history.back()` based on transaction success (`boolean`).</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> JSP Model 1アーキテクチャに基づき、Webサービスの中核である掲示板のCRUD（作成、参照、更新、削除）全サイクルを完璧に実装。データ入力フォーム（View）とDB処理専用ページ（`_ok.jsp`）を分離し、初期形態のコントローラーパターンを設計した。`SimpleDateFormat`を活用して当日に作成された記事にのみ「NEW」バッジを表示する動的UIロジックや、JSの`scrollHeight`を利用したテキストエリアの自動サイズ調整など、実務的なUXのディテールを追加。また、トランザクションの成功可否（`boolean`）に応じて`sendRedirect`とJS `history.back()`を分岐処理するバックエンドルーティング制御技法を確立した。</li></ul> |
| **05-18** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> 순수 Java 코드로 HTML을 그려내는 서블릿(Servlet) 방식의 한계를 실습하고, 이를 극복하기 위해 등장한 JSP의 내부 렌더링 생명주기(JSP &rarr; Servlet 변환 &rarr; 컴파일 &rarr; HTML 응답)를 구조적으로 완벽히 이해함. 스크립틀릿(`<% %>`)과 표현식(`<%= %>`)을 이용한 서버 사이드 데이터 바인딩을 거쳐, 최종적으로 JSTL(`<c:forEach>`)을 도입해 프레젠테이션 레이어에서 자바 코드를 분리해 내는 모던 웹 퍼블리싱의 진화 과정을 코드로 직접 체화함. 나아가 오라클 DB의 다중 테이블(EMP, DEPT) JOIN 결과를 JSP 화면에 동적으로 뿌려내는 백엔드 로직을 연동하고, CSS3의 위치 제어 속성(`relative`, `absolute`, `fixed`)과 `z-index`를 심층 학습하여 브라우저의 2차원 평면을 넘어선 3차원 공간 제어 및 레이어링 기술을 확보함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Experienced the limitations of rendering HTML using pure Java Servlets, leading to a structural understanding of the JSP rendering lifecycle (JSP &rarr; Servlet conversion &rarr; Compilation &rarr; HTML Response). Progressed from server-side data binding using scriptlets (`<% %>`) and expressions (`<%= %>`) to introducing JSTL (`<c:forEach>`), successfully separating Java logic from the presentation layer. Furthermore, integrated backend logic to dynamically render Oracle DB multi-table JOIN results (EMP, DEPT) onto JSP screens. Conducted an in-depth study of CSS3 positioning properties (`relative`, `absolute`, `fixed`) and `z-index`, securing the ability to control 3D space and element layering beyond the standard 2D document flow.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> 純粋なJavaコードでHTMLを描画するサーブレット（Servlet）方式の限界を実習し、これを克服するために登場したJSPの内部レンダリングライフサイクル（JSP &rarr; Servlet変換 &rarr; コンパイル &rarr; HTML応答）を構造的に完全に理解した。スクリプトレット（`<% %>`）と式（`<%= %>`）を用いたサーバーサイドのデータバインディングを経て、最終的にJSTL（`<c:forEach>`）を導入し、プレゼンテーションレイヤーからJavaコードを分離するモダンWebパブリッシングの進化過程をコードで直接体得。さらに、Oracle DBの複数テーブル（EMP、DEPT）JOIN結果をJSP画面に動的に表示するバックエンドロジックを連動させ、CSS3の位置制御属性（`relative`、`absolute`、`fixed`）と`z-index`を深化学習し、ブラウザの2次元平面を超えた3次元空間制御およびレイヤリング技術を確保した。</li></ul> |
| **05-19** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> JSP의 핵심 요소인 page 지시자의 다양한 속성(buffer, errorPage, contentType)을 제어하여 서버 메모리 최적화 및 안정적인 예외 처리 라우팅을 구현함. 정적 include 지시자를 활용해 반복되는 화면 요소를 분리하고 조립하는 조립식 레이아웃 아키텍처를 학습했으며, 브라우저 응답 포맷을 JSON/XML 포맷으로 변경하여 공공 데이터 Open API 공급 레이어의 기초를 정립함. 또한 request 내장 객체를 통해 클라이언트의 IP, URI 등의 서버 메타데이터를 추출하고, GET/POST 전송 방식에 따른 단일값 및 다중값(Hobby 배열)의 한글 깨짐 방지(setCharacterEncoding) 및 데이터 수신 메커니즘을 마스터함. 최종적으로 이를 싱글톤 패턴 기반의 GoodsDAO/VO 데이터 레이어와 결합하여, Flex 레이아웃 기반의 상품 목록, 블록 페이징, 그리고 상품 상세 보기 화면까지 유기적으로 이어지는 완성도 높은 웹 커머스 시스템의 데이터 파이프라인을 구축함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Mastered core JSP elements by controlling various attributes of the page directive (buffer, errorPage, contentType) to achieve server memory optimization and robust exception handling routing. Implemented a modular layout architecture by separating and assembling repeating UI components using the static include directive, and established the foundation for an Open API data delivery layer by alternating response formats into JSON and XML. Explored client-server data interaction using the request implicit object to extract server metadata (IP, URI) and handle single/multi-value form data across GET and POST methods with proper encoding alignment. Finally, integrated these concepts with a singleton-patterned GoodsDAO/VO data layer to construct a comprehensive web commerce system featuring flex-wrap product catalogs, block pagination, and interactive product detail views.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> JSPの中核要素であるpageディレクティブの多様な属性（buffer、errorPage、contentType）を制御し、サーバーメモリの最適化および安定的な例外処理ルーティングを実装。静的includeディレクティブを活用して、繰り返される画面要素を分離・結合する組込式レイアウトアーキテクチャを確立し、ブラウザへの応答フォーマットをJSON/XML形式に変更することでオープンAPIデータ提供の基礎を定立した。また、request内蔵オブジェクトを通じてクライアントのIPやURIなどのメタデータを抽出し、GET/POST送信方式に応じた単一値および複数値（配列）のデータ受信メカニズムをマスターした。最終的に、これらをシングルトンパターンベースのGoodsDAO/VOデータレイヤーと結合させ、Flexレイアウト基盤の商品一覧、ブロックページング、商品詳細画面へと有機的に繋がる完成度の高いウェブコマースシステムのデータパイプラインを構築した。</li></ul> |
| **05-20** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> JSP 내장 객체(`application`, `response`, `out`)의 생명주기를 활용해 HTTP 프로토콜을 정밀하게 제어하고, `web.xml`을 통한 글로벌 환경 설정 패턴을 구현함. 무상태(Stateless) 프로토콜을 보완하는 쿠키(Cookie) 제어 로직(`detail_before.jsp`)으로 최근 방문 기록 시스템의 기반을 설계함. 또한 HTTP 헤더 제어 및 이진 스트림 처리를 통한 파일 다운로드 기능을 구현하며 `out.clear()`를 이용한 버퍼 충돌 방어 코드를 체화함. 나아가 JSP 표준 액션 태그인 `<jsp:forward>`를 도입하여 웹의 핵심 제어 기술인 `response.sendRedirect()`와 `<jsp:forward>`의 동작 메커니즘을 비교 분석함. 클라이언트 측에서 주소를 새로 요청하여 `request` 객체를 초기화하는 리다이렉트 방식과, 서버 내부에서 `request` 객체와 전달된 파라미터 데이터를 유지한 채 화면을 전환하는 포워드 방식의 근본적인 차이를 이해하고 적재적소에 라우팅을 제어하는 웹 아키텍처 능력을 확립함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> Mastered precise HTTP control using JSP implicit objects (`application`, `response`, `out`) and implemented global configuration patterns via `web.xml`. Designed cookie state management architecture (`detail_before.jsp`) to handle user tracking across stateless connections. Developed a secure file download layer handling binary I/O streams and resolved buffer collision exceptions using `out.clear()`. Furthermore, introduced standard JSP action tags (`<jsp:forward>`) to compare page routing mechanisms against `response.sendRedirect()`. Deeply analyzed the structural difference between client-side redirection, which resets the request scope, and server-side forwarding, which preserves data payloads and request attributes while maintaining the original URL.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> JSP内蔵オブジェクト（`application`、`response`、`out`）を活用してHTTPプロトコルを精密に制御し、`web.xml`を通じたグローバル環境設定パターンを実装。無状態（Stateless）プロトコルを補完するクッキー（Cookie）制御ロジック（`detail_before.jsp`）で最近の訪問履歴システムの基盤を設計した。また、HTTPヘッダー制御およびバイナリストリーム処理によるファイルダウンロード機能を実装し、`out.clear()`を用いたバッファ衝突防衛コードを体得。さらに、JSP標準アクションタグ（`<jsp:forward>`）を導入し、`response.sendRedirect()`と`<jsp:forward>`のルーティングメカニズムを比較分析した。クライアント側でリクエストを再送して`request`オブジェクトを初期化するリダイレクト方式と、サーバー内部でデータと属性を維持したまま画面を遷移させるフォワード方式の根本的な違いを理解し、Webアーキテクチャの基盤を確立した。</li></ul> |
| **05-21** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>JSP/Servlet 아키텍처 및 렌더링 심화 학습</strong>: 단순히 기능을 구현하는 단계를 넘어, 웹 서버가 JSP를 서블릿으로 변환하고 컴파일하여 HTML을 응답하는 생명주기(Lifecycle)를 추적함. `PrintWriter`를 이용한 원시적인 서블릿 응답 방식과 JSP의 렌더링 방식 차이를 이해하고, `directive`를 활용해 서버 자원(buffer, errorPage)을 통제하는 법을 학습함. <br><strong>상태 관리 및 데이터 파이프라인</strong>: Stateless한 HTTP 프로토콜의 한계를 극복하기 위해 `Cookie`와 `Session`을 활용해 사용자의 상태를 유지하는 기법을 체화함. 특히 서버 사이드 세션과 클라이언트 사이드 쿠키의 보안적·구조적 차이를 구분하고, `detail_before.jsp`와 같은 라우팅 로직을 통해 상태 유지 시스템을 설계함. <br><strong>UI 모듈화 및 자바빈즈 패턴</strong>: 외부 템플릿을 `header`, `footer`, `aside` 등 최소 단위의 컴포넌트로 분리하고 `<jsp:include>`로 조립하는 모듈화 패턴을 습득함. 또한, 파라미터를 일일이 수신하던 방식에서 벗어나 자바빈즈(JavaBeans)와 `<jsp:setProperty>`를 통해 VO에 자동으로 데이터를 바인딩하는 객체 지향적 데이터 처리 흐름을 학습함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>JSP/Servlet Architecture</strong>: Traced the lifecycle from JSP to Servlet compilation to understand the underlying rendering mechanism beyond simple implementation. Learned to control server resources using directives. <br><strong>State Management</strong>: Mastered `Cookie` and `Session` techniques to overcome HTTP statelessness, designing state-persistence systems via routing logic. <br><strong>UI Modularization & JavaBeans</strong>: Learned to decompose static templates into reusable UI components (`header`, `footer`, `aside`) using `<jsp:include>`. Adopted the JavaBeans pattern for automatic data binding into VO objects, shifting from manual parameter handling to object-oriented data processing.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>JSP/Servletアーキテクチャ</strong>: JSPがサーブレットへ変換・コンパイルされ、HTMLを応答するレンダリングライフサイクルを構造的に理解。ディレクティブを活用したサーバーリソース制御を習得。 <br><strong>状態管理</strong>: HTTPのStatelessな特性を克服するため、CookieとSessionを使い分ける技法を体得。ルーティングロジックを通じた状態維持システムを設計。 <br><strong>UIモジュール化 & JavaBeans</strong>: 静的テンプレートをコンポーネント（header, footer, aside）に分解し、`<jsp:include>`で再利用するモジュール化パターンを学習。JavaBeansパターンを用いたVOオブジェクトへの自動データバインディングにより、オブジェクト指向的なデータ処理フローを確立した。</li></ul> |
| **05-22** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[중앙 집중형 라우팅 실습]</strong> 여러 페이지로 분산되던 요청을 `main.jsp`라는 단일 진입점으로 모으고, `jspChange.java`를 통해 URL 파라미터(`mode`)에 따라 화면을 동적으로 조립하는 프론트 컨트롤러(Front-Controller) 패턴의 초기 형태를 구현해 봄. <br><strong>[파일 업로드 처리]</strong> 텍스트 기반 폼 전송의 한계를 확인하고, `enctype="multipart/form-data"` 규약을 적용함. Servlet 3.0의 `@MultipartConfig` 어노테이션과 `Part` 객체를 활용하여 이진(Binary) 데이터를 서버의 물리적 디스크에 저장하는 과정을 실습함. <br><strong>[MyBatis 맛보기]</strong> 기존 자바 DAO 코드 내부에 하드코딩되던 SQL 쿼리의 유지보수 한계를 인지함. 이를 개선하기 위해 MyBatis 프레임워크를 가볍게 도입하여, XML 매퍼(`databoard.xml`)를 통해 비즈니스 로직과 SQL을 분리하는 '관심사의 분리'를 경험함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Routing Practice]</strong> Practiced an early form of the Front-Controller pattern by routing scattered requests through a single entry point (`main.jsp`) and dynamically assembling views using `jspChange.java` based on URL parameters (`mode`). <br><strong>[File Upload Handling]</strong> Recognized the limitations of text-based form submissions and applied the `enctype="multipart/form-data"` protocol. Implemented binary data storage to the server's physical disk using Servlet 3.0's `@MultipartConfig` and `Part` objects. <br><strong>[MyBatis Preview]</strong> Acknowledged the maintenance limitations of hardcoded SQL queries within Java DAOs. Briefly introduced the MyBatis framework to experience the 'separation of concerns' by separating business logic from SQL via XML mappers (`databoard.xml`).</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[中央集中型ルーティング実習]</strong> 分散していたリクエストを`main.jsp`という単一の入り口に集め、`jspChange.java`を通じてURLパラメータ（`mode`）に応じて画面を動的に組み立てるフロントコントローラー（Front-Controller）パターンの初期形態を実装してみた。 <br><strong>[ファイルアップロード処理]</strong> テキストベースのフォーム送信の限界を確認し、`enctype="multipart/form-data"`規約を適用。Servlet 3.0の`@MultipartConfig`アノテーションと`Part`オブジェクトを活用し、バイナリデータをサーバーの物理ディスクに保存する過程を実習した。 <br><strong>[MyBatisプレビュー]</strong> 既存のJava DAOコード内にハードコーディングされていたSQLクエリの保守の限界を認知。これを改善するためMyBatisフレームワークを軽く導入し、XMLマッパー（`databoard.xml`）を通じてビジネスロジックとSQLを分離する「関心事の分離」を経験した。</li></ul> |
| **05-26** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[View 레이어 순수성 확보]</strong> 기존 JSP 화면에 혼재되어 있던 Java 스크립틀릿(`<% %>`)과 표현식(`<%= %>`)의 유지보수 한계를 체감하고, 이를 대체하기 위해 EL(Expression Language)과 JSTL(Java Standard Tag Library)을 도입함. 객체의 프로퍼티(Getter) 접근 및 데이터 스코프 제어를 EL로 자동화하고, Java의 제어문(if, for)을 XML 기반의 JSTL Core 태그(`<c:forEach>`, `<c:choose>`)로 전환하여 HTML과의 구조적 이질감을 해소함. <br><strong>[프레젠테이션 로직의 분리]</strong> 백엔드 자바 클래스에서 수행하던 데이터 가공(날짜/숫자 포맷팅, 문자열 자르기) 로직을 JSTL의 `fmt`와 `fn` 라이브러리로 위임함. 이를 통해 백엔드는 순수 데이터를 전달하고 뷰(View)가 출력 형태를 결정하는 명확한 관심사의 분리(Separation of Concerns)를 경험하며 MVC 패턴의 기초를 확립함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[View Layer Purification]</strong> Recognized the maintenance overhead of mixing Java scriptlets (`<% %>`) with HTML. Introduced Expression Language (EL) and JSTL to decouple Java logic from the View layer. Automated property access and scope control using EL, and replaced procedural Java control flows (if, for) with declarative XML-based JSTL Core tags (`<c:forEach>`, `<c:choose>`). <br><strong>[Separation of Presentation Logic]</strong> Shifted data manipulation responsibilities (date/number formatting, string operations) from backend Java classes to the View layer using JSTL `fmt` and `fn` libraries. Experienced the core philosophy of the MVC pattern by allowing the backend to serve raw data while the View determines the presentation format.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[Viewレイヤーの純粋性確保]</strong> 既存のJSP画面に混在していたJavaスクリプトレット（`<% %>`）の保守の限界を体感し、これを代替するためにEL（Expression Language）とJSTL（Java Standard Tag Library）を導入。オブジェクトのプロパティへのアクセスやスコープ制御をELで自動化し、Javaの制御文（if, for）をXMLベースのJSTL Coreタグ（`<c:forEach>`, `<c:choose>`）に転換してHTMLとの構造的な違和感を解消した。 <br><strong>[プレゼンテーションロジックの分離]</strong> バックエンドのJavaクラスで実行していたデータ加工（日付/数値のフォーマット、文字列の切り出し）ロジックをJSTLの`fmt`および`fn`ライブラリに委任。これにより、バックエンドは純粋なデータを提供し、Viewが出力形式を決定するという明確な関心事の分離（Separation of Concerns）を経験し、MVCパターンの基礎を確立した。</li></ul> |
| **05-27** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[비즈니스 로직의 캡슐화]</strong> 기존 JSP 내부에 혼재되어 있던 DB 연동 및 페이징 연산 로직을 `FoodModel.java` 클래스로 완전히 분리함. Model 클래스가 `HttpServletRequest` 객체를 매개변수로 받아 데이터를 가공한 뒤 `setAttribute`로 바인딩하는 구조를 통해, View 레이어의 부담을 줄이고 MVC 패턴으로 나아가기 위한 과도기적 MV 아키텍처를 구현함. <br><strong>[View 레이어의 독립]</strong> `foodList.jsp`와 `foodModelList.jsp`를 대조 실습하며, Java 스크립틀릿 대신 JSTL/EL을 활용해 프론트엔드 코드를 깔끔하게 유지하는 방법을 체화함. <br><strong>[서버 데이터와 외부 API 연동]</strong> `foodModelDetail.jsp`에서 서버를 통해 DB에서 가져온 주소 데이터(`${vo.address}`)를 카카오맵(Kakao Map) 자바스크립트 API의 Geocoder 객체에 동적으로 주입하여, 백엔드 데이터가 프론트엔드 스크립트로 매끄럽게 연결되는 파이프라인을 확인함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Encapsulation of Business Logic]</strong> Extracted DB interaction and pagination logic from JSP into a dedicated `FoodModel.java` class. Implemented an intermediate MV architecture where the Model receives the `HttpServletRequest`, processes data, and binds it via `setAttribute`, significantly reducing the View layer's responsibility. <br><strong>[Independence of the View Layer]</strong> Compared `foodList.jsp` with `foodModelList.jsp` to practice maintaining clean frontend code using JSTL/EL instead of Java scriptlets. <br><strong>[Server Data & External API Integration]</strong> In `foodModelDetail.jsp`, dynamically injected server-fetched address data (`${vo.address}`) into the Kakao Map JavaScript API's Geocoder object, demonstrating a seamless data pipeline from backend to frontend scripts.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[ビジネスロジックのカプセル化]</strong> 既存のJSP内に混在していたDB連携およびページング演算ロジックを`FoodModel.java`クラスに完全に分離。Modelクラスが`HttpServletRequest`を引数として受け取り、データを加工して`setAttribute`でバインディングする構造により、Viewレイヤーの負担を軽減し、MVCパターンへ移行するための過渡期的なMVアーキテクチャを実装した。 <br><strong>[Viewレイヤーの独立]</strong> `foodList.jsp`と`foodModelList.jsp`を対照実習し、Javaスクリプトレットの代わりにJSTL/ELを活用してフロントエンドコードをクリーンに保つ方法を体得。 <br><strong>[サーバーデータと外部APIの連携]</strong> `foodModelDetail.jsp`において、DBから取得した住所データ（`${vo.address}`）をKakao Map JavaScript APIのGeocoderオブジェクトに動的に注入し、バックエンドデータがフロントエンドスクリプトへスムーズに連携されるパイプラインを確認した。</li></ul> |
| **05-28** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[계층형 답변 알고리즘 구현]</strong> 부모 글의 `group_id`, `group_step`, `group_tab`을 참조하여 답변 글의 순서와 깊이를 지정하는 로직을 구현함. 특히 중간에 답변이 끼어들 때 기존 답변들의 출력 순서(`group_step`)를 일괄 조정하는 `UPDATE` 쿼리와 새로운 답변을 `INSERT` 하는 복합적인 DB 처리 과정을 학습함. <br><strong>[비동기 통신(Ajax) 도입]</strong> 게시글 수정(`update.jsp`) 시 기존의 동기식 폼(Form) 전송 대신 jQuery의 `$.ajax`를 도입함. 화면 전체를 새로고침하지 않고 클라이언트에서 서버로 데이터를 전송한 뒤, `BoardModel`로부터 비밀번호 일치 여부(`yes`/`no`)만 텍스트 스트림으로 응답받아 동적으로 화면 이동 및 경고창을 제어하는 SPA(Single Page Application) 통신의 기초를 경험함. <br><strong>[MVC 패턴의 응답 분리]</strong> View(JSP)와 Controller(Model) 간의 역할 분리를 명확히 하여, HTML을 렌더링하는 응답과 순수 데이터(Text)만 반환하는 API 형태의 응답을 구분하여 설계함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Hierarchical Reply Algorithm]</strong> Implemented the core logic for reply boards by referencing the parent post's `group_id`, `group_step`, and `group_tab`. Learned to manage complex DB operations involving `UPDATE` queries to adjust existing reply sequences and `INSERT` queries for new replies. <br><strong>[Asynchronous Communication (Ajax)]</strong> Replaced synchronous form submissions with jQuery `$.ajax` for post updates (`update.jsp`). Experienced the basics of SPA communication by sending data to the server without page reloads and handling plain text responses (`yes`/`no`) from the `BoardModel` for password validation and dynamic routing. <br><strong>[MVC Response Separation]</strong> Clarified the separation of concerns between View and Controller, designing responses that distinguish between rendering HTML and returning pure data (API-like text streams).</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[階層型返信アルゴリズムの実装]</strong> 親記事の`group_id`、`group_step`、`group_tab`を参照し、返信記事の順序と深さを指定するロジックを実装。特に中間に返信が挿入される際、既存の返信の出力順序（`group_step`）を一括調整する`UPDATE`クエリと新規返信の`INSERT`を行う複合的なDB処理プロセスを学習した。 <br><strong>[非同期通信（Ajax）の導入]</strong> 記事修正（`update.jsp`）時に従来の同期式フォーム送信の代わりにjQueryの`$.ajax`を導入。画面全体をリロードせずにサーバーへデータを送信し、`BoardModel`からパスワードの一致結果（`yes`/`no`）のみをテキストストリームで受け取り、動的に画面遷移や警告を制御するSPA通信の基礎を経験した。 <br><strong>[MVCパターンの応答分離]</strong> View（JSP）とController（Model）間の役割分離を明確にし、HTMLをレンダリングする応答と純粋なデータ（Text）のみを返すAPI形式の応答を区別して設計した。</li></ul> |
| **05-29** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[Front-Controller 아키텍처 도입]</strong> 모든 클라이언트의 요청(`*.do`)을 단일 진입점인 `Controller` 서블릿으로 모아 중앙 통제하는 라우팅 아키텍처를 구현함. 공통 인터페이스(`Model.java`)를 활용한 다형성(Polymorphism)과 Command 패턴을 적용하여, 컨트롤러가 개별 비즈니스 로직의 구현체를 몰라도 유연하게 실행할 수 있는 구조를 설계함. <br><strong>[IoC 컨테이너 및 Reflection 체화]</strong> 코드 내부에 하드코딩된 객체 생성(`new`) 방식의 결합도 문제를 인지함. 이를 해결하기 위해 XML(`application.xml`) 파싱과 Java Reflection(`Class.forName()`)을 융합하여, 런타임에 동적으로 객체를 생성하고 Map에 주입하는 Spring Framework의 의존성 주입(DI) 원리를 순수 자바로 실습함. <br><strong>[동적 템플릿 레이아웃]</strong> `main.jsp`를 전체 UI의 뼈대로 설정하고, Model에서 분기 처리한 뷰 경로를 `${main_jsp}` 변수로 받아 동적으로 화면을 스위칭하는 효율적인 프레젠테이션 레이어 패턴을 적용함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Front-Controller Architecture]</strong> Implemented a central routing architecture by funneling all client requests (`*.do`) through a single entry point (`Controller` Servlet). Applied polymorphism and the Command pattern via a common interface (`Model.java`), allowing the controller to execute business logic flexibly without hardcoded dependencies. <br><strong>[IoC Container & Reflection]</strong> Addressed tight coupling issues caused by hardcoded object instantiation (`new`). Implemented the core philosophy of Spring's Dependency Injection by parsing XML configurations and utilizing Java Reflection (`Class.forName()`) to dynamically instantiate objects at runtime. <br><strong>[Dynamic Template Layout]</strong> Established `main.jsp` as the structural UI frame, using EL (`${main_jsp}`) to dynamically inject and switch content views based on paths determined by the Model layer.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[Front-Controller アーキテクチャの導入]</strong> 全てのクライアント要求（`*.do`）を単一の入り口である`Controller`サーブレットに集約し、中央制御するルーティングアーキテクチャを実装。共通インターフェース（`Model.java`）を活用した多態性（Polymorphism）とCommandパターンを適用し、コントローラーが個別のビジネスロジックを知らなくても柔軟に実行できる構造を設計した。 <br><strong>[IoCコンテナとReflectionの体得]</strong> コード内にハードコーディングされたオブジェクト生成（`new`）による結合度の問題を認知。これを解決するため、XMLパースとJava Reflection（`Class.forName()`）を融合させ、ランタイムに動的にオブジェクトを生成しMapに注入するSpring Frameworkの依存性注入（DI）の原理を純粋なJavaで実習した。 <br><strong>[動的テンプレートレイアウト]</strong> `main.jsp`をUI全体の骨組みとし、Modelで決定されたビューのパスを`${main_jsp}`変数で受け取り、動的に画面を切り替える効率的なプレゼンテーションレイヤーパターンを適用した。</li></ul> |
| **06-01** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[View 레이어의 순수성 확보]</strong> `boardList`, `boardDetail` 등 모든 JSP 화면에서 자바 스크립틀릿(`<% %>`)을 완전히 배제하고, JSTL과 EL만을 활용해 컨트롤러가 넘겨준 데이터를 렌더링하는 진정한 의미의 MVC View 구성을 완료함. <br><strong>[파일 I/O 스트림 및 다운로드 구현]</strong> `download.jsp`를 통해 브라우저의 기본 텍스트 렌더링을 차단하고(`Content-Disposition`), `application.getRealPath()`로 서버의 물리적 경로를 추적함. `BufferedInputStream`과 `OutputStream`을 연결하여 바이너리 데이터를 클라이언트 로컬 디스크로 안전하게 밀어내는 백엔드 스트림 제어를 실습함. <br><strong>[jQuery 기반의 동적 UI 토글]</strong> `boardDetail.jsp`에서 삭제 버튼 클릭 시 화면 전환 없이 jQuery의 `show()`, `hide()`를 이용해 숨겨진 폼(`<tr style="display:none">`)을 동적으로 제어하는 기초적인 프론트엔드 상호작용을 경험함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[View Layer Purification]</strong> Completely eliminated Java scriptlets (`<% %>`) from all JSP views (`boardList`, `boardDetail`), utilizing only JSTL and EL to render data passed by the controller, finalizing a strict MVC View architecture. <br><strong>[File I/O Stream & Download]</strong> Implemented `download.jsp` to bypass default browser text rendering using the `Content-Disposition` header. Tracked physical server paths via `application.getRealPath()` and utilized `BufferedInputStream/OutputStream` to securely pipe binary data to the client's local disk. <br><strong>[jQuery-based UI Toggling]</strong> Experienced basic frontend interaction in `boardDetail.jsp` by using jQuery `show()` and `hide()` to dynamically toggle hidden form rows without requiring a page reload.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[Viewレイヤーの純粋性確保]</strong> `boardList`や`boardDetail`など全てのJSP画面からJavaスクリプトレット（`<% %>`）を完全に排除し、JSTLとELのみを活用してコントローラーから渡されたデータをレンダリングする、真の意味でのMVC View構成を完了した。 <br><strong>[ファイルI/Oストリームとダウンロード実装]</strong> `download.jsp`を通じてブラウザのデフォルトテキストレンダリングを遮断し（`Content-Disposition`）、`application.getRealPath()`でサーバーの物理パスを追跡。`BufferedInputStream`と`OutputStream`を接続し、バイナリデータをクライアントのローカルディスクへ安全に転送するバックエンドのストリーム制御を実習した。 <br><strong>[jQueryベースの動的UI制御]</strong> `boardDetail.jsp`にて、削除ボタンのクリック時に画面遷移なしでjQueryの`show()`、`hide()`を用いて隠しフォーム（`<tr style="display:none">`）を動的に制御する基礎的なフロントエンドインタラクションを経験した。</li></ul> |
| **06-02** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[어노테이션 기반 라우팅 및 Reflection]</strong> 기존 `Map` 기반의 Command 패턴을 폐기하고, 사용자 정의 어노테이션(`@Controller`, `@RequestMapping`)과 리플렉션을 활용해 런타임에 동적으로 URL과 메서드를 매핑하는 구조를 도입함. 공통 인터페이스(`Model.java`)의 제약 없이 하나의 클래스 내에 여러 매핑 메서드를 배치하는 POJO 기반의 모던 MVC 패턴을 구현함. <br><strong>[MyBatis 모듈화 및 설정 외부화]</strong> `food-mapper.xml`에서 반복되는 쿼리를 `<sql>` 태그로 분리하여 `<include>`로 재사용하는 기법을 실습함. 또한 `Config.xml`에 하드코딩되던 DB 접속 정보를 `db.properties`로 분리하여 보안을 강화하고 배포 환경 변화에 유연하게 대응할 수 있는 구조를 마련함. <br><strong>[동적 템플릿 레이아웃 고도화]</strong> `header.jsp`와 `main.jsp`를 분리 연동하여, 컨트롤러의 요청(`*.do`)에 따라 `main.jsp`의 본문 영역(`${main_jsp}`)만 동적으로 렌더링되도록 처리함. 전체 페이지 갱신 없이 UI 컴포넌트를 조립하는 레이아웃 패턴을 안착시킴.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Annotation-based Routing & Reflection]</strong> Replaced the Map-based Command pattern with custom annotations (`@Controller`, `@RequestMapping`) and Reflection for dynamic URL-to-method mapping at runtime. Implemented a POJO-based modern MVC pattern allowing multiple mapping methods per class without interface constraints. <br><strong>[MyBatis Modularization & External Config]</strong> Modularized repetitive queries in `food-mapper.xml` using `<sql>` and `<include>` tags. Extracted hardcoded DB credentials from `Config.xml` into `db.properties` to enhance security and deployment flexibility. <br><strong>[Refined Dynamic Layout]</strong> Modularized UI into `header.jsp` and `main.jsp`. Implemented a dynamic layout pattern where only the main content area (`${main_jsp}`) is re-rendered based on routing, establishing a component-based UI architecture.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[アノテーションベースのルーティングとReflection]</strong> 従来のMapベースのCommandパターンを廃棄し、独自アノテーション（`@Controller`、`@RequestMapping`）とリフレクションを活用して動的にURLとメソッドをマッピングする構造を導入。共通インターフェースの制約なしに1つのクラス内に複数のマッピングメソッドを配置できるPOJOベースのモダンMVCパターンを実装した。<br><strong>[MyBatisのモジュール化と設定の外部化]</strong> `food-mapper.xml`内の反復クエリを`<sql>`タグで分離し`<include>`で再利用する技法を実習。また、`Config.xml`にハードコーディングされていたDB接続情報を`db.properties`に分離し、セキュリティの強化とデプロイ環境への柔軟な対応を可能にした。<br><strong>[動的テンプレートレイアウトの高度化]</strong> `header.jsp`と`main.jsp`をモジュール化し、ルーティングに応じて本文領域（`${main_jsp}`）のみを動的にレンダリングするコンポーネントベースのUIレイアウトパターンを定着させた。</li></ul> |
| **06-04** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[외부 API 연동 (Kakao Map)]</strong> `foodDetail.jsp`에서 서버가 DB로부터 조회하여 넘겨준 텍스트 주소 데이터(`${vo.address}`)를 카카오맵(Kakao Map) JavaScript API의 `Geocoder` 객체에 동적으로 주입함. 백엔드 데이터와 프론트엔드 외부 라이브러리 간의 파이프라인을 구축하여 지도 위 좌표로 변환(Geocoding) 및 마커 렌더링 로직을 구현함. <br><strong>[동적 템플릿 레이아웃 정립]</strong> `index.jsp` 진입 시 즉시 `main.do` 컨트롤러로 리다이렉트 시키고, `main.jsp`를 뼈대로 삼아 `<jsp:include page="${main_jsp }">`를 통해 본문 내용만 동적으로 교체하는 UI 컴포넌트화 패턴을 안착시킴. <br><strong>[View 계층의 순수성 유지]</strong> `home.jsp`의 페이징 처리 및 썸네일 그리드 출력 과정에서 자바 스크립틀릿(`<% %>`)을 배제하고 JSTL(`<c:forEach>`, `<c:if>`)과 EL만을 활용하여 프레젠테이션 로직의 가독성과 독립성을 확보함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[External API Integration]</strong> In `foodDetail.jsp`, dynamically injected server-fetched address data (`${vo.address}`) into the Kakao Map JavaScript API's `Geocoder`. Established a data pipeline between the backend and a frontend external library to perform geocoding and render map markers. <br><strong>[Dynamic Template Layout]</strong> Configured `index.jsp` to redirect immediately to the `main.do` controller, establishing `main.jsp` as the structural template. Utilized `<jsp:include page="${main_jsp }">` to dynamically swap content components. <br><strong>[View Layer Purification]</strong> Eliminated Java scriptlets from `home.jsp` during pagination and thumbnail grid rendering, exclusively relying on JSTL and EL to maintain the presentation layer's independence and readability.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[外部API連携 (Kakao Map)]</strong> `foodDetail.jsp`において、サーバーがDBから取得した住所データ（`${vo.address}`）をKakao Map JavaScript APIの`Geocoder`オブジェクトに動的に注入。バックエンドデータとフロントエンド外部ライブラリ間のデータパイプラインを構築し、地図上の座標への変換（Geocoding）およびマーカーのレンダリングロジックを実装した。<br><strong>[動的テンプレートレイアウトの定着]</strong> `index.jsp`アクセス時に直ちに`main.do`コントローラーへリダイレクトさせ、`main.jsp`を骨組みとして`<jsp:include page="${main_jsp }">`を通じて本文内容のみを動的に入れ替えるUIコンポーネント化パターンを定着させた。<br><strong>[View階層の純粋性維持]</strong> `home.jsp`のページング処理やサムネイルグリッド出力過程からJavaスクリプトレット（`<% %>`）を排除し、JSTLとELのみを活用することでプレゼンテーションロジックの可読性と独立性を確保した。</li></ul> |
| **06-05** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[JavaScript 기초 문법]</strong> 기존 자바 백엔드 로직에서 벗어나, 브라우저 단에서 동작하는 JS 기초를 학습함. `var`의 호이스팅 한계를 극복하는 `let`과 `const`의 블록 스코프 개념을 이해하고, 자바스크립트 특유의 동치 연산자(`===`), 타입 형변환(`Number()`), 예외 데이터(`NaN`, `Infinity`) 처리 방식을 실습함. <br><strong>[배열과 객체 순회]</strong> 전통적인 `for`문을 넘어, 컬렉션 처리에 최적화된 모던 JS 순회 문법(`for...in`, `for...of`, `forEach`, `map`)을 적용하여 배열 데이터를 화면에 출력하는 과정을 코드로 체화함. <br><strong>[Linux 인프라 구축 및 배포]</strong> 리눅스 기본 명령어와 환경 설정에 익숙해지기 위해 기존 Ubuntu OS를 완전히 초기화(포맷)하고 재설치함. 이후 순수 환경에서 Tomcat WAS를 직접 세팅하고 작성한 웹 애플리케이션을 배포(Deploy)하는 일련의 서버 운영 사이클을 점검함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[JavaScript Fundamentals]</strong> Transitioned to frontend scripting, studying vanilla JS. Grasped block-scoping with `let` and `const`, JS-specific strict equality (`===`), type conversions, and handling anomalies like `NaN` and `Infinity`. <br><strong>[Array/Object Iteration]</strong> Moved beyond traditional loops to implement modern JS iteration methods (`for...in`, `for...of`, `forEach`, `map`) for rendering collection data. <br><strong>[Linux Infrastructure & Deployment]</strong> Wiped and reinstalled the Ubuntu OS to practice basic Linux commands and system configuration. Manually provisioned a Tomcat server from scratch and successfully performed deployment tests, solidifying understanding of the server operations cycle.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[JavaScript 基礎文法]</strong> ブラウザ上で動作するJSの基礎を学習。`let`と`const`によるブロックスコープの概念を理解し、JS特有の厳密等価演算子（`===`）、型変換、例外データ（`NaN`、`Infinity`）の処理方式を実習した。<br><strong>[配列とオブジェクトの反復処理]</strong> 従来の`for`文を超え、コレクション処理に最適化されたモダンJSの反復構文（`for...in`、`for...of`、`forEach`、`map`）を適用し、配列データを画面に出力するプロセスをコードで体得した。<br><strong>[Linux インフラ構築とデプロイ]</strong> Linuxの基本コマンドと環境設定に慣れるため、既存のUbuntu OSを完全に初期化（フォーマット）して再インストール。その後、クリーンな環境でTomcat WASを手動でセットアップし、Webアプリケーションのデプロイテストを行う一連のサーバー運用サイクルを確認した。</li></ul> |
| **06-08** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[RESTful 데이터 파이프라인 구축]</strong> 자바 백엔드가 JSP 화면(HTML)을 반환하던 전통적인 방식에서 탈바꿈하여, `JSONArray`와 `JSONObject`를 통해 순수한 데이터 데이터 스트림(JSON)만 브라우저로 쏴주는 API 엔드포인트(`EmpModel.java`)를 구현함. <br><strong>[Axios 비동기 통신 및 DOM 바인딩]</strong> 클라이언트 사이드에서 `axios.get()`을 통해 백엔드 및 외부 OpenAPI(영화진흥위원회)의 JSON 데이터를 비동기로 수집함. 수집된 객체 배열을 모던 JS 순회 함수(`map()`)를 통해 순수 가공한 후, `querySelector`와 `innerHTML` 등 Vanilla JS의 DOM 제어 메커니즘을 이용해 화면의 새로고침 없이 프론트엔드 UI를 동적으로 조립함. <br><strong>[배열 구조 및 화살표 함수 체화]</strong> `slice()`, `find()`, `push()` 등 실무 페이징 및 유효성 검증에 필수적인 배열 조작 메서드를 익히고, 스코프 한계가 명확한 화살표 함수(Arrow Function)를 체화하여 모던 프론트엔드 프레임워크(React/Vue)로 진입하기 위한 초석을 다짐.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[RESTful Data Pipeline Construction]</strong> Shifted from the traditional serverside HTML rendering to creating API endpoints (`EmpModel.java`) that serialize Java collections into `JSONArray` and `JSONObject` to stream pure JSON data to the client. <br><strong>[Axios Async Comm & DOM Binding]</strong> Utilized `axios.get()` on the client side to asynchronously fetch JSON streams from both local backends and external OpenAPIs (KOBIS). Processed the returned object arrays using modern JS iteration (`map()`) and dynamically manipulated the UI without page reloads through Vanilla JS DOM mechanisms such as `querySelector` and `innerHTML`. <br><strong>[Array Manipulation & Arrow Functions]</strong> Mastered essential array manipulation techniques (`slice`, `find`, `push`) required for client-side pagination and validation, while adopting Arrow Functions to build a solid foundation for modern frontend ecosystems (React/Vue).</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[RESTfulデータパイプラインの構築]</strong> サーバ側でJSP画面（HTML）をレンダリングする従来の方法から脱却し、`JSONArray`と`JSONObject`を用いて純粋なデータストリーム（JSON）のみをクライアントに返すAPIエンドポイント（`EmpModel.java`）を実装した。<br><strong>[Axios非同期通信とDOMバインディング]</strong> クライアント側で`axios.get()`を呼び出し、ローカルのバックエンドおよび外部のOpenAPIからJSONデータを非同期で取得。取得したオブジェクト配列をモダンJSの反復関数（`map()`）で処理し、画面のリロードなしに`querySelector`や`innerHTML`などのVanilla JSのDOM制御メカニズムを介してUIを動的に組み立てた。<br><strong>[配列操作とアロー関数の体得]</strong> クライアントサイドのページングやバリデーションに不可欠な配列操作メソッド（`slice`、`find`、`push`）を習得し、アロー関数を採用することで、モダンフロントエンドフレームワーク（React/Vue）へ移行するための強固な基盤を構築した。</li></ul> |
| **06-09** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[상용 3rd-Party API 연동]</strong> 포트원(Iamport) 결제 모듈과 다음(Daum) 우편번호 검색 API를 프로젝트에 통합함. 백엔드를 거치지 않고 프론트엔드 단에서 외부 스크립트를 로드하여 상용 서비스와 직접 통신하고 콜백(Callback)을 처리하는 실무적인 아키텍처를 실습함. <br><strong>[클라이언트 사이드 실시간 필터링]</strong> `music.js`를 통해 서버에서 한 번 받아온 JSON 데이터를 브라우저 메모리에 캐싱하고, `keyup/input` 이벤트와 `indexOf`를 활용해 즉각적인 클라이언트 사이드 검색(Client-side Filtering)을 구현함. 이는 매번 DB에 `LIKE` 쿼리를 날리는 방식보다 서버 부하를 획기적으로 줄이는 모던 웹 최적화 기법임. <br><strong>[DOM 요소의 동적 생명주기 제어]</strong> `js_12.jsp`에서 파일 업로드 행(Row)을 동적으로 추가 및 삭제하는 기능을 구현함. `document.createElement`, `appendChild`, `remove` 등의 바닐라 JS 메서드를 통해, 화면 새로고침 없이 브라우저 메모리 상의 DOM 트리를 직접 조작하고 상태(`fileIndex`)를 관리하는 프론트엔드 렌더링의 핵심 원리를 체화함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[3rd-Party API Integration]</strong> Integrated PortOne (Iamport) payment module and Daum Postcode API. Practiced practical architecture by loading external scripts on the frontend to communicate directly with commercial services and handle callbacks without backend intermediary. <br><strong>[Client-Side Real-time Filtering]</strong> Implemented client-side filtering in `music.js` by caching fetched JSON data in browser memory. Utilized `keyup/input` events and `indexOf` for instantaneous search functionality, significantly reducing server load compared to repetitive DB `LIKE` queries. <br><strong>[Dynamic DOM Lifecycle Management]</strong> Implemented dynamic addition and removal of file upload rows in `js_12.jsp`. Mastered core frontend rendering principles by directly manipulating the DOM tree in memory using Vanilla JS methods (`createElement`, `appendChild`, `remove`) and managing state (`fileIndex`) without page reloads.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[商用3rd-Party API連携]</strong> PortOne（Iamport）決済モジュールとDaum郵便番号検索APIをプロジェクトに統合。バックエンドを経由せず、フロントエンド側で外部スクリプトをロードし、商用サービスと直接通信してコールバックを処理する実践的なアーキテクチャを実習した。<br><strong>[クライアントサイドのリアルタイムフィルタリング]</strong> `music.js`において、サーバーから取得したJSONデータをブラウザのメモリにキャッシュし、`keyup/input`イベントと`indexOf`を活用した即時的なクライアントサイド検索（Client-side Filtering）を実装。これは、毎回DBに`LIKE`クエリを発行する方式に比べ、サーバー負荷を劇的に軽減するモダンWebの最適化技法である。<br><strong>[DOM要素の動的ライフサイクル制御]</strong> `js_12.jsp`にて、ファイルアップロード行の動的追加および削除機能を実装。`document.createElement`、`appendChild`、`remove`などのVanilla JSメソッドを通じて、画面のリロードなしにブラウザメモリ上のDOMツリーを直接操作し、状態（`fileIndex`）を管理するフロントエンドレンダリングの核心原理を体得した。</li></ul> |
| **06-10** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[BOM 기반 팝업 통신에서 임베디드 레이어로의 진화]</strong> 전통적인 `window.open()`과 `opener` 객체를 이용해 자식 창이 부모 창의 Form 요소를 역추적·제어하는 1세대 팝업 통신 방식을 구현한 후, 이를 현재 화면 내에 동적으로 삽입되는 다음(Daum) 우편번호 검색 API 기반의 임베디드 레이어 방식으로 리팩토링하여 사용자 경험(UX)을 최적화함. <br><strong>[jQuery 도입 및 DOM 추상화]</strong> Vanilla JS의 복잡한 노드 탐색(`querySelectorAll`) 및 반복문 처리를 jQuery의 강력한 셀렉터와 메서드 체이닝(`$('h3').css()`)으로 대체하여 프론트엔드 생산성을 극대화함. `text()`, `html()`, `val()`, `attr()` 등 핵심 조작 메서드를 비교 체화함. <br><strong>[화살표 함수 내 Lexical 'this' 바인딩 제어]</strong> jQuery 이벤트 핸들러 내에서 화살표 함수(Arrow Function)를 사용할 경우, 고전적 `this`가 자신이 속한 컨텍스트(window)를 가리키는 스코프 한계를 인지하고, 이벤트 대상을 정확히 추적하기 위해 `e.currentTarget`을 활용하는 고급 자바스크립트 문법의 예외 케이스를 정교하게 해결함. <br><strong>[jQuery 4 규약 기반 이벤트 리스너]</strong> 고전적인 `$().click()` 방식에서 벗어나 차세대 표준인 `$().on('keydown', ...)` 리스너 사양을 적용하고, 엔터 키(`e.keyCode === 13`) 조작을 통한 비동기 채팅형 템플릿과 `append() / remove()`를 활용한 동적 파일 업로드 행 제어 로직을 완성함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Evolution from BOM Popups to Embedded Component Layers]</strong> Implemented a traditional 1st-generation popup communication layout using `window.open()` and `opener` to track and mutate parent form states. Upgraded this workflow by integrating the Daum Postcode API as an embedded, single-page dynamic layer, highly optimizing user experience (UX). <br><strong>[jQuery Integration & DOM Abstraction]</strong> Replaced raw Vanilla JS node iteration (`querySelectorAll` loops) with concise jQuery selectors and method chaining (`$('h3').css()`). Compared and internalized core manipulation APIs: `text()`, `html()`, `val()`, and `attr()`. <br><strong>[Lexical 'this' Scope Resolution in Arrow Functions]</strong> Recognized the lexical scope limits of Arrow Functions where `this` binds to the outer window object rather than the triggering DOM element inside jQuery handlers. Resolved this advanced JS edge case by utilizing `e.currentTarget` to accurately track event sources. <br><strong>[Modern Event Listeners based on jQuery 4 specs]</strong> Moved away from legacy `$().click()` syntax to adopt the standard `$().on('keydown', ...)` listener specification. Built an interactive chat UI parsing the Enter key (`e.keyCode === 13`) and established dynamic row injection workflows using `append()` and `remove()`.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[BOMポップアップ通信から組み込みレイヤーへの進化]</strong> 伝統的な`window.open()`と`opener`オブジェクトを利用して、子ウィンドウが親ウィンドウのForm要素を逆追跡・制御する第1世代のポップアップ通信方式を実装。その後、現在の画面内に動的に挿入されるDaum郵便番号検索APIベースの組み込みレイヤー方式にリファクタリングし、ユーザー体験（UX）を最適化した。<br><strong>[jQueryの導入とDOMの抽象化]</strong> Vanilla JSの複雑なノード探索（`querySelectorAll`ループ）を、簡潔なjQueryセレクターとメソッドチェーン（`$('h3').css()`）に置き換え、生産性を極大化。`text()`、`html()`、`val()`、`attr()`などの核心操作メソッドを比較・体得した。<br><strong>[アロー関数内のLexical 'this' バインディング制御]</strong> jQueryイベントハンドラー内でアロー関数を使用する際、従来の`this`がアウターコンテキスト（window）を指すスコ프の制限を認知。イベント対象を正確に追跡するために`e.currentTarget`を活用する、高度なJavaScript文法のエッジケースを精緻に解決した。<br><strong>[jQuery 4規約ベースのイベントリスナー]</strong> 古い`$().click()`構文から脱却し、次世代標準である`$().on('keydown', ...)`リスナー仕様を適用。エンターキー（`e.keyCode === 13`）操作を伴うチャット型UIの構築や、`append() / remove()`を活用した動的なファイルアップロード行の制御ロジックを完成させた。</li></ul> |
| **06-11** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[AJAX 코어 아키텍처 이해]</strong> jQuery나 Axios 같은 라이브러리에 의존하기 전, 바닐라 JS의 `XMLHttpRequest` 객체를 활용해 HTTP 통신 파이프라인(open, send, onreadystatechange)을 직접 구현함(`ajax.jsp`). 이를 통해 비동기 통신의 저수준(Low-level) 동작 원리를 체화한 후, jQuery의 `$.ajax()`로 추상화하여 생산성을 높이는 과정을 비교 실습함. <br><strong>[클라이언트 사이드 렌더링(CSR) 전환]</strong> JSTL을 이용해 서버에서 HTML을 완성해 내려보내던 기존 방식(`foodList.jsp`)에서 벗어나, 빈 HTML 뼈대만 로드한 뒤 AJAX로 순수 JSON 데이터를 받아 브라우저 단에서 동적으로 화면을 조립하는 CSR 아키텍처(`list_ajax.jsp`, `ajax.js`)를 구현함. <br><strong>[비동기 동적 페이징 구현]</strong> 전체 화면의 새로고침 없이, 페이징 버튼 클릭 시 AJAX를 통해 해당 페이지의 데이터만 백엔드에 요청하고 `innerHTML`로 목록 영역(`#print`)과 페이지네이션 영역(`#pagination`)만 부분 갱신하는 SPA(Single Page Application) 형태의 부드러운 화면 제어를 완성함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Core AJAX Architecture]</strong> Implemented the HTTP communication pipeline using raw `XMLHttpRequest` (`ajax.jsp`) to understand the low-level mechanics of asynchronous requests before utilizing abstracted libraries. Successfully transitioned to jQuery's `$.ajax()` to boost productivity while maintaining structural understanding. <br><strong>[Transition to Client-Side Rendering (CSR)]</strong> Shifted from traditional Server-Side Rendering (SSR using JSTL in `foodList.jsp`) to a CSR approach (`list_ajax.jsp`, `ajax.js`). Loaded an empty HTML shell and dynamically constructed the UI in the browser using JSON data fetched via AJAX. <br><strong>[Asynchronous Dynamic Pagination]</strong> Implemented SPA-like pagination behavior. Clicking pagination links fetches subset data via AJAX and updates only the specific DOM nodes (`#print` and `#pagination`) using `innerHTML`, eliminating full page reloads and enhancing user experience.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[AJAXコアアーキテクチャの理解]</strong> jQueryやAxiosなどのライブラリに依存する前に、Vanilla JSの`XMLHttpRequest`オブジェクトを活用してHTTP通信パイプラインを直接実装（`ajax.jsp`）。非同期通信の低レベルな動作原理を体得した後、jQueryの`$.ajax()`による抽象化で生産性を高める過程を比較実習した。<br><strong>[クライアントサイドレンダリング(CSR)への移行]</strong> JSTLを用いてサーバー側でHTMLを完成させる従来方式（`foodList.jsp`）から脱却。空のHTML骨組みのみをロードし、AJAXで純粋なJSONデータを受け取ってブラウザ側で動的に画面を組み立てるCSRアーキテクチャ（`list_ajax.jsp`、`ajax.js`）を実装した。<br><strong>[非同期の動的ページング実装]</strong> 画面全体のリロードなしに、ページングボタンのクリック時にAJAXを通じて該当ページのデータのみをバックエンドに要求し、`innerHTML`を用いてリスト領域（`#print`）とページネーション領域（`#pagination`）のみを部分更新するSPA形態の滑らかな画面制御を完成させた。</li></ul> |
| **06-12** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[MyBatis 동적 테이블 바인딩]</strong> `goods-mapper.xml`에서 테이블명을 하드코딩하지 않고, 클라이언트의 요청(`type`)에 따라 `goods_all`, `goods_best` 등의 테이블을 유동적으로 선택할 수 있도록 MyBatis의 `${table}` 문법을 적용함. 값(Value)을 바인딩하는 `#{}`과 식별자(Identifier)를 바인딩하는 `${}`의 동작 차이를 실무적으로 체화함. <br><strong>[Model 계층 내 비즈니스 로직 캡슐화]</strong> `GoodsModel.java` 내에서 데이터베이스가 반환한 텍스트 형태의 가격 데이터(`30,000원` 등)를 정규식(`replaceAll("[^0-9]", "")`)으로 파싱하여 순수 정수형으로 변환함. 이후 할인율과 실제 판매가를 Java 단에서 직접 계산하여 View(`detail.jsp`)로 전달함으로써, View 레이어가 순수 출력 역할에만 집중하도록 설계함. <br><strong>[리눅스 환경 원격 배포 실습]</strong> 로컬 환경에서 완성된 MVC 프로젝트를 Git Repository에 푸시한 뒤, 원격 Ubuntu Linux 서버에서 `git clone`으로 소스를 내려받아 Tomcat WAS에 직접 배포(Deploy)하는 서버 운영 파이프라인의 기초 사이클을 확립함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Dynamic Table Binding in MyBatis]</strong> Applied MyBatis `${table}` syntax in `goods-mapper.xml` to dynamically switch target tables (`goods_all`, `goods_best`, etc.) based on client requests, grasping the critical distinction between value binding (`#{}`) and identifier injection (`${}`). <br><strong>[Business Logic Encapsulation in Model]</strong> Processed string-formatted price data directly within `GoodsModel.java` using Regular Expressions (`replaceAll("[^0-9]", "")`). Calculated exact discount rates and final prices in the Java layer before passing them to `detail.jsp`, adhering to strict MVC separation of concerns where Views only handle rendering. <br><strong>[Linux Remote Deployment Practice]</strong> Pushed the local MVC project to a Git repository, utilized `git clone` on a remote Ubuntu Linux server, and manually deployed the application to a Tomcat WAS, establishing a foundational CI/CD and server operations cycle.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[MyBatisの動的テーブルバインディング]</strong> `goods-mapper.xml`において、クライアントの要求に応じてターゲットテーブル（`goods_all`、`goods_best`など）を動的に切り替えるため、MyBatisの`${table}`構文を適用。値のバインディング（`#{}`）と識別子の注入（`${}`）の動作の違いを実践的に体得した。<br><strong>[Model階層におけるビジネスロジックのカプセル化]</strong> `GoodsModel.java`内で、テキスト形式の価格データ（`30,000円`など）を正規表現（`replaceAll("[^0-9]", "")`）を用いて純粋な整数型に解析。Java側で直接割引率と最終価格を計算してから`detail.jsp`へ渡すことで、Viewが純粋な出力機能のみに集中できる設計を実現した。<br><strong>[Linux環境へのリモートデプロイ実習]</strong> ローカルで作成したMVCプロジェクトをGitにプッシュした後、リモートのUbuntu Linuxサーバーにて`git clone`でソースを取得し、手動でTomcat WASへデプロイ（Deploy）するサーバー運用パイプラインの基礎サイクルを確立した。</li></ul> |
| **06-15** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[AJAX 기반 비동기 인증 및 세션 제어]</strong> `header.jsp`와 `MainModel` 간의 AJAX 통신을 통해 화면 새로고침 없는 로그인/로그아웃 파이프라인을 구축함. 백엔드는 순수 상태 문자열("OK", "NOID", "NOPWD")만 반환하고, 클라이언트(JQuery)가 이를 파싱하여 UI 분기와 에러 핸들링을 주도하는 모던 웹 인증 구조를 체화함. <br><strong>[Cookie를 활용한 비순차적 상태 유지]</strong> `detail_before.do` 전처리 라우터를 도입하여 사용자가 클릭한 상품 번호를 브라우저의 Cookie에 캐싱한 후 상세 페이지로 리다이렉트시키는 '최근 본 항목' 알고리즘을 구현함. HTTP의 무상태성(Stateless)을 보완하면서 서버 DB의 부하를 줄이는 클라이언트 사이드 스토리지 활용법을 익힘. <br><strong>[뷰 계층의 동적 권한 렌더링 및 MyBatis 제어]</strong> JSTL의 `sessionScope`를 참조하여 로그인 상태 여부와 작성자 본인 일치 여부(`rvo.id==sessionScope.id`)에 따라 댓글 폼과 수정/삭제 버튼을 동적으로 렌더링함. 더불어 MyBatis `<selectKey>`를 활용하여 Insert 실행 전 `MAX(no)+1` PK 값을 트랜잭션 내부에서 안전하게 자동 채번하는 무결성 로직을 적용함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[AJAX-based Async Authentication & Session Control]</strong> Built a seamless login/logout pipeline using AJAX between `header.jsp` and `MainModel` without full page reloads. The backend acts as a pure API returning status strings ("OK", "NOID"), delegating UI branching and error handling to the jQuery client. <br><strong>[Cookie-driven Stateless Tracking]</strong> Implemented a 'Recently Viewed Items' algorithm by introducing a pre-processing router (`detail_before.do`) that caches item IDs in browser Cookies before redirecting. Mastered client-side storage to compensate for HTTP statelessness while reducing database load. <br><strong>[Dynamic View Authorization & MyBatis Tuning]</strong> Dynamically rendered reply forms and edit/delete controls based on JSTL `sessionScope` conditions (e.g., verifying `rvo.id==sessionScope.id`). Enhanced DB integrity by utilizing MyBatis `<selectKey>` to safely auto-generate PKs (`MAX(no)+1`) within the transaction prior to execution.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[AJAXベースの非同期認証とセッション制御]</strong> `header.jsp`と`MainModel`間のAJAX通信により、画面リロードなしのログイン・ログアウトパイプラインを構築。バックエンドは純粋なステータス文字列（"OK", "NOID"）のみを返し、クライアント（jQuery）がUIの分岐とエラーハンドリングを主導するモダンなWeb認証構造を体得した。<br><strong>[Cookieを活用した状態維持]</strong> `detail_before.do`という前処理ルーターを導入し、ユーザーがクリックした商品番号をブラウザのCookieにキャッシュしてから詳細ページへリダイレクトする「最近見た項目」アルゴリズムを実装。HTTPの無状態性（Stateless）を補完しつつサーバーDBの負荷を軽減するクライアントサイドストレージの活用法を習得した。<br><strong>[View階層の動的権限レンダリングとMyBatis制御]</strong> JSTLの`sessionScope`を参照し、ログイン状態や作成者本人との一致（`rvo.id==sessionScope.id`）に応じてコメントフォームや修正・削除ボタンを動的にレンダリング。さらに、MyBatisの`<selectKey>`を活用し、Insert実行前に`MAX(no)+1`のPK値をトランザクション内で安全に自動採番する整合性ロジックを適用した。</li></ul> |
| **06-16** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[모던 비동기 통신 도입]</strong> `movie.jsp`에서 `fetch` API와 `async/await`를 활용하여 YouTube 검색 API를 연동함. 기존 콜백 기반의 비동기 제어에서 벗어나, 동기적 코드 흐름으로 네트워크 통신을 제어하고 예외(`try-catch`)를 처리하는 모던 JS 패턴을 적용함. <br><strong>[CSR 기반 동적 검색 및 페이징]</strong> `find.jsp`에서 서버사이드 렌더링을 배제하고, AJAX 통신으로 수신한 JSON 데이터를 바탕으로 클라이언트 단에서 그리드와 페이지네이션 HTML을 직접 조립해 DOM에 주입하는 완전한 CSR(Client-Side Rendering) 아키텍처를 구현함. <br><strong>[인라인 UI 상태 제어]</strong> `detail.jsp`의 댓글 수정 기능에서 화면 리로드 없이 jQuery의 `.show()`, `.hide()`와 커스텀 데이터 속성(`data-no`)을 활용해 특정 행(Row)의 폼 상태만 독립적으로 토글(Toggle)하는 컴포넌트 상태 제어 기법을 체화함. <br><strong>[체크박스 배열 전송과 동적 SQL 연동]</strong> `list.jsp`에서 다중 선택된 체크박스 데이터를 배열(`name="data"`) 형태로 백엔드에 전송하고, 이를 MyBatis의 `<foreach>` 구문과 연동하여 `IN (...)` 조건절로 일괄 조회하는 데이터 파이프라인을 확립함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Modern Asynchronous Communication]</strong> Integrated the YouTube Search API in `movie.jsp` using the `fetch` API combined with `async/await`. Transitioned from traditional callback-based flows to a modern, synchronous-looking paradigm for better readability and robust error handling (`try-catch`). <br><strong>[CSR-Driven Search & Pagination]</strong> Eliminated server-side rendering in `find.jsp`. Fully implemented Client-Side Rendering (CSR) by parsing fetched JSON data to dynamically construct grid layouts and pagination controls directly within the browser's memory. <br><strong>[Inline UI State Management]</strong> Managed specific row states in `detail.jsp` for comment editing. Utilized jQuery `.show()`/`.hide()` alongside HTML5 `data-no` attributes to independently toggle target DOM elements without page reloads, mirroring modern component-based UI behaviors. <br><strong>[Array Payload & Dynamic SQL]</strong> Established a data pipeline in `list.jsp` transmitting multi-selected checkbox arrays (`name="data"`) to the backend, seamlessly coupling it with MyBatis `<foreach>` tags to execute dynamic `IN (...)` queries.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[モダン非同期通信の導入]</strong> `movie.jsp`にて`fetch` APIと`async/await`を活用し、YouTube検索APIを連携。従来のコールバックベースの非同期制御から脱却し、同期的なコードフローでネットワーク通信を制御し例外（`try-catch`）を処理するモダンJSパターンを適用した。<br><strong>[CSRベースの動的検索とページング]</strong> `find.jsp`においてサーバーサイドレンダリングを排除。AJAX通信で受信したJSONデータをもとに、クライアント側でグリッドやページネーションHTMLを直接組み立てDOMに注入する、完全なCSR（Client-Side Rendering）アーキテクチャを実装した。<br><strong>[インラインUI状態制御]</strong> `detail.jsp`のコメント修正機能において、画面の再読み込みなしにjQueryの`.show()`、`.hide()`およびカスタムデータ属性（`data-no`）を活用。特定の行（Row）のフォーム状態のみを独立してトグルするコンポーネント状態制御技法を体得した。<br><strong>[チェックボックス配列送信と動的SQL連携]</strong> `list.jsp`にて複数選択されたチェックボックスデータを配列（`name="data"`）としてバックエンドに送信し、これをMyBatisの`<foreach>`構文と連携させて`IN (...)`条件句で一括照会するデータパイプラインを確立した。</li></ul> |
| **06-17** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[MyBatis 동적 쿼리의 고도화]</strong> `food-mapper.xml`에서 `<trim>`, `<foreach>`, `<choose>` 태그를 복합적으로 사용하여 다중 체크박스(한식, 양식 등) 선택에 따른 동적 `OR` 조건절을 생성함. 쿼리 스트링을 자바 코드에서 조립하지 않고 DB 매퍼 계층에서 안전하게 파싱하는 모던 ORM 기법을 체화함. <br><strong>[AJAX 배열 직렬화 제어]</strong> 프론트엔드(`find.jsp`)에서 다중 선택된 체크박스 배열 데이터를 백엔드로 전송할 때, jQuery AJAX의 `traditional: true` 옵션을 적용하여 파라미터 키값에 `[]`가 붙는 기본 직렬화 현상을 방지하고 Java 컨트롤러가 `getParameterValues()`로 정확히 매핑할 수 있는 파이프라인을 구축함. <br><strong>[CSR 기반 복합 상태 유지]</strong> 검색 조건(`ss`), 필터 속성(`type`), 현재 페이지(`page`) 등 다중 상태값을 JSON으로 주고받으며, 화면의 새로고침 없이 DOM 트리만 부분 갱신하는 완전한 클라이언트 사이드 렌더링(CSR)을 구현함. <br><strong>[Ubuntu 24.04 인프라 구축]</strong> 최신 LTS 버전의 리눅스 운영체제를 바닥부터 설치하고 네트워크 및 WAS(Tomcat) 환경을 세팅하여 향후 무중단 배포 및 클라우드(AWS) 아키텍처로 넘어가기 위한 서버 엔지니어링 기반을 다짐.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Advanced MyBatis Dynamic SQL]</strong> Utilized a complex combination of `<trim>`, `<foreach>`, and `<choose>` tags in `food-mapper.xml` to dynamically generate `OR` clauses based on multi-select checkbox inputs. Mastered modern ORM techniques by delegating query assembly to the mapper layer rather than concatenating strings in Java. <br><strong>[AJAX Array Serialization Control]</strong> Configured `traditional: true` in jQuery AJAX (`find.jsp`) when transmitting array payloads to the backend. This suppressed the default behavior of appending `[]` to keys, ensuring accurate data binding via Java's `getParameterValues()`. <br><strong>[CSR-based Complex State Management]</strong> Implemented a full Client-Side Rendering (CSR) architecture. Transmitted complex state variables (search keywords, filter arrays, pagination) via JSON to dynamically mutate the DOM without full page reloads. <br><strong>[Ubuntu 24.04 Infrastructure Setup]</strong> Provisioned a fresh installation of the latest Ubuntu LTS, configuring the network and WAS (Tomcat) environments. Established a solid infrastructure baseline for future cloud deployments and CI/CD pipelines.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[MyBatis動的SQLの高度化]</strong> `food-mapper.xml`にて、`<trim>`、`<foreach>`、`<choose>`タグを複合的に使用し、複数チェックボックスの選択に応じた動的な`OR`条件句を生成。Javaコード内でクエリ文字列を結合せず、DBマッパー階層で安全に解析するモダンORM技法を体得した。<br><strong>[AJAX配列シリアライズ制御]</strong> フロントエンド（`find.jsp`）から複数選択された配列データを送信する際、jQuery AJAXの`traditional: true`オプションを適用。パラメータキーに`[]`が付与されるデフォルトの直列化を防ぎ、Javaコントローラーが正確にマッピングできるパイプラインを構築した。<br><strong>[CSRベースの複合状態維持]</strong> 検索条件、フィルター属性、現在のページなどの複数状態をJSONで送受信し、画面のリロードなしにDOMツリーのみを部分更新する完全なクライアントサイドレンダリング（CSR）を実装した。<br><strong>[Ubuntu 24.04 インフラ構築]</strong> 最新LTSバージョンのLinux OSをクリーンインストールし、ネットワークおよびWAS（Tomcat）環境をセットアップ。今後のクラウドアーキテクチャへの移行を見据えたサーバーエンジニアリングの基盤を固めた。</li></ul> |
| **06-18** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[상용 UI 템플릿 통합]</strong> 상용 테마(Yummy 기반)의 복잡한 CSS/JS 자원을 `main.jsp`에 집중적으로 로드하고, 내부 콘텐츠 영역(`${main_jsp}`)만 동적으로 교체하는 SPA 형태의 레이아웃 템플릿 패턴을 완벽하게 이식함. <br><strong>[View 레이어의 세밀한 모듈화]</strong> 전체 화면을 `header.jsp`(GNB 및 소셜/로그인 제어), `footer.jsp`(카피라이트), `cookie.jsp`(인스타그램 갤러리/최근 항목) 등으로 조각내어 조립하는 컴포넌트 기반 UI 아키텍처를 구축하여 코드 중복을 제거하고 유지보수성을 극대화함. <br><strong>[시각적 계층 및 UX 컴포넌트 적용]</strong> 화면 진입 시의 Preloader(로딩 애니메이션), 사이트 이동 경로를 알려주는 Breadcrumb 내비게이션, 그리고 직관적인 Pagination(페이지네이션) UI를 적용하여 클라이언트 사이드의 사용자 경험(UX)을 실무 수준으로 끌어올림.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Professional UI Template Integration]</strong> Successfully integrated a commercial-grade UI theme (Yummy-based). Centralized external CSS/JS resource loading in `main.jsp` while dynamically swapping the core content area (`${main_jsp}`), mimicking an SPA-like layout pattern. <br><strong>[Granular View Layer Modularization]</strong> Fragmented the monolithic view into reusable components such as `header.jsp` (GNB/Auth), `footer.jsp` (Copyright), and `cookie.jsp` (Gallery widgets). Established a component-based UI architecture to eliminate code duplication and maximize maintainability. <br><strong>[Enhanced Visual Hierarchy & UX]</strong> Applied advanced UX components including Preloaders for state transitions, Breadcrumb navigation for pathfinding, and polished Pagination styles, elevating the frontend user experience to industry standards.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[商用UIテンプレートの統合]</strong> 商用テーマ（Yummyベース）の複雑なCSS/JSリソースを`main.jsp`に集中的にロードし、内部コンテンツ領域（`${main_jsp}`）のみを動的に入れ替えるSPA形式のレイアウトテンプレートパターンを完全に移植した。<br><strong>[Viewレイヤーの詳細なモジュール化]</strong> 全画面を`header.jsp`（GNBおよびログイン制御）、`footer.jsp`（コピーライト）、`cookie.jsp`（ギャラリーウィジェット）などに分割して組み立てるコンポーネントベースのUIアーキテクチャを構築。コードの重複を排除し保守性を極大化した。<br><strong>[視覚的階層とUXコンポーネントの適用]</strong> 画面アクセス時のPreloader（ローディングアニメーション）、サイトの移動経路を示すBreadcrumbナビゲーション、そして直感的なPagination（ページネーション）UIを適用し、クライアントサイドのユーザー体験（UX）を実務レベルに引き上げた。</li></ul> |
| **06-19** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[Multipart 파일 업로드 및 디스크 I/O 제어]</strong> 일반 텍스트 데이터와 바이너리 파일이 혼합된 폼 데이터를 전송하기 위해 `enctype="multipart/form-data"`를 적용함. 프론트엔드 컨트롤러(`DispatcherServlet`)와 분리된 `UploadServlet`을 통해 `Part` 인터페이스로 물리적 디스크에 파일을 기록하고 DB 무결성을 유지하는 파이프라인을 구축함. <br><strong>[HTTP 헤더 조작 및 스트림(Stream) 기반 다운로드]</strong> `DataBoardModel`의 다운로드 라우터(`download.do`)에서 `Content-Disposition` 헤더를 `attachment`로 조작하여 브라우저의 파일 다운로드를 트리거함. `BufferedInputStream`과 `BufferedOutputStream`을 활용해 1024바이트(1KB) 단위 청크(Chunk)로 데이터를 전송하여 서버 메모리 병목을 방지함. <br><strong>[가상 번호(Virtual Numbering) 페이징]</strong> 목록 화면(`list.jsp`)에서 DB의 실제 PK(`no`) 대신 `총 레코드 수 - ((현재 페이지-1) * 페이지당 행 수)` 연산을 통한 가상 번호(`${count}`)를 렌더링하여 중간 데이터 삭제 시에도 시각적 연속성을 보장하는 UX를 구현함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Multipart Upload & Disk I/O Control]</strong> Applied `multipart/form-data` for mixed payloads. Separated file processing into `UploadServlet`, utilizing the `Part` interface to physically write binary files to disk and sync metadata with the database. <br><strong>[HTTP Header Manipulation & Stream Download]</strong> Configured the `Content-Disposition: attachment` HTTP header in `DataBoardModel` to trigger browser downloads. Utilized `BufferedInputStream` and `BufferedOutputStream` to transfer files in 1KB chunks, preventing server memory overflow. <br><strong>[Virtual Numbering Pagination]</strong> Implemented virtual numbering (`${count}`) in UI data grids instead of raw PKs, ensuring sequentially decreasing numbers even when intermediate records are deleted, adhering to UI/UX best practices.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[MultipartアップロードとディスクI/O制御]</strong> テキストとバイナリが混在するペイロードのために`multipart/form-data`を適用。フロントコントローラーから分離された`UploadServlet`にて、`Part`インターフェースを活用し物理ディスクへのファイル書き込みとDBメタデータの同期を行うパイプラインを構築した。<br><strong>[HTTPヘッダー操作とストリームベースのダウンロード]</strong> `DataBoardModel`のダウンロード機能にて、`Content-Disposition: attachment`ヘッダーを設定しブラウザのダウンロードをトリガー。`BufferedInputStream`と`BufferedOutputStream`を用いて1KBのチャンク単位でデータを転送し、サーバーメモリのボトルネックを防止した。<br><strong>[仮想番号(Virtual Numbering)ページング]</strong> リスト画面にてDBの実際のPKの代わりに演算による仮想番号をレンダリング。中間データ削除時にも視覚的な連続性を保証するUI/UXを実装した。</li></ul> |
| **06-22** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[다중 뎁스 템플릿 아키텍처]</strong> `main.jsp` 내부에서 `mypage_main.jsp` 또는 `admin_main.jsp`를 로드하고, 세부 콘텐츠 영역을 동적으로 교체하는 2중 레이아웃(Nested Template) 구조를 구현하여 UI 컴포넌트의 재사용성을 극대화함. <br><strong>[연쇄적 비동기(Cascade AJAX) 파이프라인]</strong> `reserve_main.jsp`라는 단일 페이지 내에서 맛집 선택 $\rightarrow$ 달력 호출 $\rightarrow$ 시간 조회 $\rightarrow$ 인원 선택으로 이어지는 예약 프로세스를 화면 새로고침 없이 연쇄적인 AJAX 통신으로 제어하는 모던 SPA 아키텍처를 구현함. <br><strong>[클라이언트 DOM 상태 관리]</strong> 각 예약 단계별로 선택된 값(식당명, 날짜, 시간, 인원)을 자바스크립트 전역 변수가 아닌 브라우저 DOM 트리(`#food_reserve_...`)에 직접 렌더링하여 임시 상태(State)로 캐싱하고, 최종 완성 시에만 서버로 Submit 하도록 설계하여 서버 메모리 부하를 방지함. <br><strong>[백엔드 캘린더 엔진 및 난수 제어]</strong> `java.util.Calendar`를 활용해 월별 1일의 요일과 마지막 날짜를 동적으로 연산하는 엔진을 구축하고, `Math.random()`과 `Arrays.sort()`를 통해 중복 없는 예약 가능 시간대를 무작위 추출하여 실서비스 환경을 시뮬레이션함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Nested Template Architecture]</strong> Implemented a double-depth layout architecture maximizing UI component reusability by dynamically swapping core content areas within `mypage_main.jsp` and `admin_main.jsp`. <br><strong>[Cascading AJAX Pipeline]</strong> Built a modern SPA architecture in `reserve_main.jsp` where the reservation flow (Restaurant -> Calendar -> Time -> Headcount) progresses sequentially via chained AJAX requests without any full page reloads. <br><strong>[DOM-based State Management]</strong> Managed temporary reservation states directly within the DOM tree instead of global JS variables. This ensures the server is only hit with a final submit when the entire payload is ready, reducing unnecessary memory overhead. <br><strong>[Backend Calendar & Randomization Engine]</strong> Engineered a custom calendar logic using `java.util.Calendar` to compute accurate date grids, alongside a randomization algorithm (`Math.random()`, `Arrays.sort()`) to simulate real-world available time slots.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[多重デプステンプレートアーキテクチャ]</strong> `main.jsp`内で`mypage_main.jsp`や`admin_main.jsp`をロードし、詳細コンテンツ領域を動的に入れ替える2重レイアウト構造を実装してUIの再利用性を極大化した。<br><strong>[連鎖的非同期(Cascade AJAX)パイプライン]</strong> `reserve_main.jsp`という単一ページ内で、レストラン選択 $\rightarrow$ カレンダー呼び出し $\rightarrow$ 時間照会 $\rightarrow$ 人数選択へと続く予約プロセスを、画面リロードなしに連鎖的なAJAX通信で制御するSPAアーキテクチャを実装した。<br><strong>[クライアントDOM状態管理]</strong> 各予約段階で選択された値（店名、日付、時間、人数）をJSのグローバル変数ではなく、DOMツリー（`#food_reserve_...`）に直接レンダリングして一時状態としてキャッシュし、最終完了時にのみサーバーへSubmitする設計でサーバー負荷を防止した。<br><strong>[バックエンドカレンダーエンジンと乱数制御]</strong> `java.util.Calendar`を活用して月ごとの1日の曜日と最終日を動的に演算するエンジンを構築し、`Math.random()`と`Arrays.sort()`を用いて重複のない予約可能時間帯を無作為に抽出し、本番環境をシミュレートした。</li></ul> |
| **06-23** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[Vue.js 기반 MVVM 아키텍처 도입]</strong> jQuery의 직접적인 DOM 제어 방식에서 벗어나, 상태(State) 데이터의 변경이 View(화면)에 즉각적으로 동기화되는 양방향 데이터 바인딩(`v-model`) 및 선언적 렌더링(`v-for`, `v-if`)을 구현함. <br><strong>[컴포넌트 단위 UI 모듈화(CBA)]</strong> 단일 JSP 파일에 밀집되어 있던 뷰 로직을 `page_card.js`(페이징)와 `food_detail.js`(상세보기)로 독립시키고, 부모 컴포넌트의 데이터를 `props`를 통해 주입받는 모던 프론트엔드 컴포넌트 아키텍처를 체화함. <br><strong>[WAR 배포 및 ngrok 터널링 인프라 구축]</strong> 로컬에서 개발한 MVC 애플리케이션을 WAR 형식으로 패키징하여 Ubuntu 서버의 Tomcat WAS에 독립 배포함. 이후 내부망/NAT 환경의 한계를 극복하기 위해 `ngrok`을 활용한 포트 포워딩 터널링을 구성하여, 외부(Windows) 클라이언트에서 접근 가능한 실서비스 테스트 환경을 확립함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Vue.js MVVM Architecture]</strong> Transitioned from imperative jQuery DOM manipulation to a declarative, data-driven rendering model using Vue.js. Implemented two-way data binding (`v-model`) and structural directives (`v-for`, `v-if`) to reactively sync State with the View. <br><strong>[Component-Based Architecture (CBA)]</strong> Fragmented monolithic JSP views into reusable standalone UI components (`page_card.js`, `food_detail.js`). Mastered the flow of injecting parent state into child components via `props`. <br><strong>[WAR Deployment & ngrok Tunneling]</strong> Packaged the MVC application into a WAR file and successfully deployed it to a Tomcat WAS on an Ubuntu server. Configured `ngrok` port forwarding to bypass local NAT/Firewall restrictions, exposing the local server to external environments (Windows) for real-world testing.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[Vue.jsベースのMVVMアーキテクチャ導入]</strong> jQueryによる直接的なDOM操作から脱却し、状態（State）データの変更が即座にView（画面）に同期される双方向データバインディング（`v-model`）および宣言的レンダリング（`v-for`、`v-if`）を実装した。<br><strong>[コンポーネント単位のUIモジュール化]</strong> 単一のJSPに密集していたビューのロジックを`page_card.js`（ページング）と`food_detail.js`（詳細表示）に分離。親コンポーネントのデータを`props`を通じて注入する、モダンフロントエンドのコンポーネントアーキテクチャを体得した。<br><strong>[WARデプロイとngrokトンネリング構築]</strong> ローカルで開発したMVCアプリケーションをWAR形式でパッケージ化し、UbuntuサーバーのTomcat WASにデプロイ。さらに`ngrok`を活用してポートフォワーディングを行い、内部ネットワークの制限を越えて外部（Windows）からアクセス可能なテスト環境を確立した。</li></ul> |
| **06-24** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[서비스 계층(Service Layer) 도입]</strong> `GoodsModel`(컨트롤러)과 `GoodsDAO`(데이터 접근) 사이에 `GoodsService` 인터페이스와 `GoodsServiceImpl` 구현체를 배치하는 실무형 아키텍처를 도입함. 이를 통해 컨트롤러는 요청/응답 제어에만 집중하고, 데이터베이스 트랜잭션 및 비즈니스 로직은 서비스 계층이 전담하도록 구조적 결합도를 낮춤(Loose Coupling). <br><strong>[Vue.js 기반 상품 도메인 SPA 마이그레이션]</strong> 상품 목록(`list.jsp`), 검색(`find.jsp`), 상세 보기(`detail.jsp`) 화면을 순수 Vue 3(MVVM) 기반으로 전면 개편함. Axios를 통해 백엔드 API와 통신하고, `v-for`, `v-if`, `v-model` 등의 디렉티브를 활용하여 클라이언트 브라우저에서 화면을 즉각적으로 렌더링하는 SPA(Single Page Application) 파이프라인을 구축함. <br><strong>[MyBatis resultMap 자동 매핑]</strong> `goods-mapper.xml`에서 `<resultMap autoMapping="true">` 속성을 활용하여, DB 컬럼명과 VO 객체의 필드명이 일치하는 데이터는 자동 바인딩되도록 처리하고 불일치하는 필드만 수동 매핑함으로써 ORM(Object-Relational Mapping)의 생산성을 최적화함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Service Layer Introduction]</strong> Introduced a practical Service Layer architecture by decoupling `GoodsModel` (Controller) and `GoodsDAO` (Data Access) using the `GoodsService` interface and its implementation (`GoodsServiceImpl`). This enforces the Single Responsibility Principle and paves the way for advanced transaction management. <br><strong>[Vue.js SPA Migration for Goods Domain]</strong> Completely refactored the Goods list, search, and detail views using Vue 3. Established an SPA pipeline where Axios handles API communication and Vue directives (`v-model`, `v-for`) dynamically render the UI on the client side. <br><strong>[MyBatis Auto-Mapping]</strong> Optimized ORM productivity in `goods-mapper.xml` by utilizing `<resultMap autoMapping="true">`, allowing MyBatis to implicitly map identical column-field pairs while only requiring explicit mapping for mismatches.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[サービス階層(Service Layer)の導入]</strong> `GoodsModel`（コントローラ）と`GoodsDAO`（データアクセス）の間に`GoodsService`インターフェースとその実装クラス（`GoodsServiceImpl`）を配置する実務型アーキテクチャを導入。これにより、コントローラは要求/応答の制御に集中し、ビジネスロジックとDBトランザクションはサービス階層が専担するよう結合度を低下（Loose Coupling）させた。<br><strong>[Vue.jsベースの商品ドメインSPA移行]</strong> 商品一覧、検索、詳細画面を純粋なVue 3（MVVM）ベースに全面改修。Axiosを通じてバックエンドAPIと通信し、ディレクティブを活用してクライアント側で即座にレンダリングするSPAパイプラインを構築した。<br><strong>[MyBatis resultMapの自動マッピング]</strong> `goods-mapper.xml`にて`<resultMap autoMapping="true">`を活用し、DBカラム名とVOのフィールド名が一致するデータは自動バインディングさせ、不一致のフィールドのみを手動マッピングすることでORMの生産性を最適化した。</li></ul> |
| **06-25** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[Jackson 기반 JSON 직렬화]</strong> 기존의 `json-simple` 라이브러리를 이용한 수동 JSON 조립 방식에서 벗어나, 실무 표준인 `com.fasterxml.jackson.databind.ObjectMapper`를 도입함. Java의 `Map` 콜렉션을 `writeValueAsString()`을 통해 즉각적으로 JSON 텍스트로 변환하는 직렬화 자동화 파이프라인을 구축함. <br><strong>[Vue 3 ES Module(ESM) 도입]</strong> `list.jsp`에서 전통적인 전역 스크립트 로드 방식을 배제하고, `<script type="importmap">`과 `<script type="module">`을 활용하여 Vue 객체를 독립된 모듈로 `import` 하는 모던 프론트엔드 생태계의 표준 모듈 시스템을 브라우저 상에 완벽하게 이식함. <br><strong>[비동기 삭제 파이프라인 및 상태 기반 UI 토글]</strong> `detail.jsp`에서 삭제 버튼 클릭 시 화면 이동 없이 상태 변수(`bShow`)를 반전시켜 비밀번호 폼을 동적으로 노출(Toggle)함. Axios를 이용한 백엔드 검증 실패 시, `this.$refs`를 통해 특정 물리 노드로 커서를 강제 복귀시키는 디테일한 에러 핸들링을 가상 돔(Virtual DOM) 환경 내에서 구현함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Jackson-based JSON Serialization]</strong> Transitioned from manual JSON assembly (`json-simple`) to the industry-standard `ObjectMapper` (Jackson). Automated the serialization pipeline by mapping Java `HashMap` collections directly into JSON strings using `writeValueAsString()`. <br><strong>[Vue 3 ES Modules Integration]</strong> Replaced legacy global script loading with `<script type="importmap">` and `<script type="module">`. Successfully implemented modern JavaScript ES Modules (ESM) to scope and import Vue components natively. <br><strong>[Async Deletion Pipeline & State-driven UI Toggle]</strong> Dynamically exposed the password verification form in `detail.jsp` without page reloads by toggling a state variable (`bShow`). Integrated Axios for backend validation, utilizing `this.$refs` to accurately shift the focus back to the input node upon failure, demonstrating advanced UX control within a Virtual DOM ecosystem.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[JacksonベースのJSONシリアライズ]</strong> 従来の`json-simple`を用いた手動JSON組み立て方式から脱却し、実務標準である`ObjectMapper`（Jackson）を導入。Javaの`Map`コレクションを`writeValueAsString()`を通じて即座にJSONテキストへ変換する直列化の自動化パイプラインを構築した。<br><strong>[Vue 3 ES Module(ESM)の導入]</strong> 伝統的なグローバルスクリプトのロード方式を排除し、`<script type="importmap">`と`<script type="module">`を活用。Vueオブジェクトを独立したモジュールとして`import`するモダンフロントエンドの標準モジュールシステムを完全に移植した。<br><strong>[非同期削除パイプラインと状態ベースのUIトグル]</strong> `detail.jsp`にて、削除ボタンクリック時に画面遷移なしで状態変数（`bShow`）を反転させ、パスワード入力フォームを動的に露出（Toggle）。Axiosによるバックエンド検証に失敗した場合、`this.$refs`を用いて物理的な入力ノードへフォーカスを強制復帰させる、仮想DOM環境下での緻密なUXおよび例外処理を実装した。</li></ul> |
| **06-26** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[상태 기반 데이터 수정(Update) 파이프라인]</strong> `update.jsp`에서 Axios로 기존 게시글 데이터를 비동기 조회한 후, Vue.js의 양방향 바인딩(`v-model`)을 활용하여 수정 폼에 마운트(Mount)함. 사용자 입력에 따라 실시간으로 변이된 상태(State)를 백엔드로 전송하는 모던 MVVM 폼 제어 방식을 구현함. <br><strong>[비즈니스 로직의 완벽한 캡슐화]</strong> 게시글 수정 및 삭제 시 필수적인 '비밀번호 검증' 로직을 컨트롤러나 프론트엔드가 아닌 `BoardServiceImpl` (서비스 계층) 내부로 격리함. DB를 조회하여 해시/평문 일치 여부를 판단한 뒤에만 트랜잭션을 발생시키는 견고한 보안 아키텍처를 체화함. <br><strong>[클라이언트 주도형 라우팅 (Client-side Routing)]</strong> 수정 완료(`update_ok.do`) 후의 화면 전환을 서버사이드의 `redirect`에 의존하지 않고, Axios 통신의 `.then()` 콜백 내부에서 `window.location.href`를 조작하여 클라이언트가 능동적으로 상세 페이지로 복귀하는 로직을 적용함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[State-driven Update Pipeline]</strong> Completed the Vue.js CRUD cycle in `update.jsp`. Asynchronously fetched existing board data via Axios and mounted it onto the update form using two-way data binding (`v-model`). Successfully managed state mutations and transmitted updated payloads back to the server. <br><strong>[Encapsulation of Business Logic]</strong> Isolated the critical 'password verification' logic within the `BoardServiceImpl` (Service Layer). Evaluated credentials against the database prior to executing any `UPDATE` or `DELETE` transactions, adhering strictly to secure backend architecture patterns. <br><strong>[Client-side Routing Control]</strong> Shifted the responsibility of post-update navigation from server-side redirects to the client. Utilized `window.location.href` within the Axios `.then()` callback to seamlessly route the user back to the updated detail view.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[状態ベースのデータ修正パイプライン]</strong> `update.jsp`にて、既存の掲示板データをAxiosで非同期照会し、Vue.jsの双方向バインディング（`v-model`）を活用して修正フォームにマウント。ユーザー入力に応じてリアルタイムに変異した状態（State）をバックエンドへ送信するモダンMVVMフォーム制御を実装した。<br><strong>[ビジネスロジックの完全なカプセル化]</strong> 記事の修正および削除に不可欠な「パスワード検証」ロジックを、コントローラやフロントエンドではなく`BoardServiceImpl`（サービス階層）内部に隔離。DBと照合して一致を判断した後にのみトランザクションを発生させる堅牢なセキュリティアーキテクチャを体得した。<br><strong>[クライアント主導のルーティング]</strong> 修正完了後（`update_ok.do`）の画面遷移をサーバーサイドの`redirect`に依存せず、Axios通信の`.then()`コールバック内で`window.location.href`を操作し、クライアントが能動的に詳細ページへ復帰するロジックを適用した。</li></ul> |
| **06-29** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[Vue 컴포넌트 통신 (Props & Emit)]</strong> 페이지네이션 UI를 `pagecard.js`라는 독립된 Vue 컴포넌트로 분리함. 부모 컴포넌트(`list.jsp`)로부터 `props`를 통해 페이징 상태 데이터를 하향식으로 주입받고, 클릭 이벤트 발생 시 `$emit`을 통해 상향식으로 부모의 `pageChange` 메서드를 트리거하는 단방향 데이터 흐름(One-way Data Flow) 아키텍처를 구현함. <br><strong>[MVVM 내 3rd-Party API 통합]</strong> `detail.jsp`의 Vue 인스턴스 메서드(`buyBtn`) 내부에 포트원(Iamport) 결제 모듈을 통합함. 데이터 바인딩된 상태 값(`this.vo.goods_name`, `this.vo.price`)을 외부 API 파라미터로 즉각 주입하여 결제 파이프라인을 제어함. <br><strong>[백엔드 팩토리 패턴 및 Service 표준화]</strong> 분산되어 있던 `SqlSessionFactory`를 중앙 싱글톤 객체(`CreateSqlSessionFactory`)로 통합하고, `Member` 및 `Goods` 도메인 전반에 Service 인터페이스를 도입하여 컨트롤러와 데이터 접근 계층 간의 결합도를 극단적으로 낮춤(Loose Coupling). <br><strong>[Vue 생명주기 및 디렉티브 체화]</strong> `goods.jsp`와 `vue_1.jsp`를 통해 Vue의 가상 돔(Virtual DOM) 렌더링 원리, 라이프사이클(`mounted`, `created`), 그리고 필수 디렉티브(`v-for`, `v-model`, `v-if`)의 동작 방식을 실무적으로 문서화하고 체화함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Vue Component Communication (Props & Emit)]</strong> Extracted the pagination UI into a standalone Vue component (`pagecard.js`). Implemented a strict one-way data flow where the parent (`list.jsp`) injects state via `props`, and the child triggers parent methods via `$emit` upon user interaction. <br><strong>[3rd-Party API in MVVM]</strong> Integrated the Iamport payment gateway directly within the Vue instance methods (`detail.jsp`), dynamically injecting reactive state variables (`this.vo.price`) into the payment API payload. <br><strong>[Backend Factory & Service Standardization]</strong> Centralized `SqlSessionFactory` instantiation into a singleton and standardized the Service Layer across `Member` and `Goods` domains, achieving loose coupling between Controllers and DAOs. <br><strong>[Vue Lifecycle & Directives Mastery]</strong> Documented and internalized Vue's Virtual DOM mechanics, component lifecycles (`mounted`, `created`), and core directives (`v-for`, `v-model`) within `goods.jsp` and `vue_1.jsp`.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[Vueコンポーネント通信 (Props & Emit)]</strong> ページネーションUIを`pagecard.js`という独立したVueコンポーネントに分離。親（`list.jsp`）から`props`を通じて状態データを下向きに注入し、クリック時に`$emit`を通じて上向きに親のメソッドをトリガーする単方向データフローを実装した。<br><strong>[MVVM内の3rd-Party API統合]</strong> `detail.jsp`のVueインスタンスメソッド内にIamport決済モジュールを統合。データバインディングされた状態値（`this.vo.price`など）を外部APIのパラメータとして即座に注入し、決済パイプラインを制御した。<br><strong>[バックエンドファクトリーとService標準化]</strong> 分散していた`SqlSessionFactory`を中央シングルトンオブジェクトに統合し、各ドメインにServiceインターフェースを導入して、コントローラとDAO間の結合度を極端に低下させた。<br><strong>[Vueライフサイクルとディレクティブの体得]</strong> 仮想DOMのレンダリング原理、ライフサイクル（`mounted`など）、および必須ディレクティブの動作方式を実務的に文書化し体得した。</li></ul> |
| **06-30** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[자체 제작 MVC 프레임워크 완성]</strong> `@Controller`와 `@RequestMapping` 어노테이션을 활용하여 클라이언트의 요청(URI)을 동적으로 매핑하는 프론트 컨트롤러(DispatcherServlet) 기반의 자체 MVC 아키텍처를 완성함. 스프링 프레임워크의 코어 동작 원리를 바닥부터 구현하여 제어의 흐름을 완벽히 장악함. <br><strong>[비즈니스 계층(Service)과 ORM(MyBatis)의 결합]</strong> `Controller(Model) -> Service -> DAO -> MyBatis`로 이어지는 4계층 백엔드 파이프라인을 확립함. 동적 쿼리(`<trim>`, `<choose>`)와 자동 매핑(`autoMapping`)을 활용해 DB 접근을 최적화하고, 비즈니스 로직을 Service 인터페이스로 캡슐화하여 유지보수성을 극대화함. <br><strong>[점진적 SPA 마이그레이션 (Vue 3 + Axios)]</strong> 기존 JSP/JSTL 기반의 동기식 렌더링 화면을 Vue.js 기반의 비동기 컴포넌트 아키텍처로 전면 개편함. 부모-자식 간 단방향 데이터 통신(`props`, `$emit`)과 `v-model`을 활용한 상태(State) 제어로 화면 깜빡임 없는 모던 웹 UX를 구현함. <br><strong>[서드파티 API 기반 도메인 확장]</strong> 카카오맵(Kakao Map) API를 통한 좌표-주소 변환(Geocoder), 아임포트(Iamport)를 활용한 인라인 결제 시스템, 유튜브 API 비동기 검색 등 실무 상용 서비스에서 요구하는 외부 인터페이스 연동 로직을 Vue 생명주기 내부에 완벽히 이식함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Custom MVC Framework Finalization]</strong> Completed a custom Front Controller (DispatcherServlet) architecture using `@Controller` and `@RequestMapping` annotations to dynamically route HTTP requests. Internalized the core mechanics of the Spring Framework. <br><strong>[Service Layer & ORM Integration]</strong> Established a robust 4-tier backend pipeline (`Model -> Service -> DAO -> MyBatis`). Optimized data access using dynamic SQL and encapsulated business logic within Service interfaces for maximum maintainability. <br><strong>[Progressive SPA Migration (Vue 3)]</strong> Refactored legacy JSP/JSTL synchronous views into a modern asynchronous SPA architecture using Vue.js and Axios. Implemented strict one-way data flow (`props`, `$emit`) and reactive state management (`v-model`). <br><strong>[3rd-Party API Integrations]</strong> Seamlessly integrated essential commercial APIs (Kakao Maps Geocoder, Iamport Payment Gateway, YouTube Data API) directly within the Vue component lifecycles, expanding the domain capabilities to enterprise standards.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[独自MVCフレームワークの完成]</strong> `@Controller`と`@RequestMapping`アノテーションを活用し、クライアントの要求（URI）を動的にマッピングするフロントコントローラ基盤の独自MVCアーキテクチャを完成。Springフレームワークのコアドライブ原理をゼロから実装した。<br><strong>[ビジネス階層(Service)とORMの結合]</strong> `Model -> Service -> DAO -> MyBatis`へと続く4階層バックエンドパイプラインを確立。動的SQLと自動マッピングを活用してDBアクセスを最適化し、ビジネスロジックをServiceインターフェースにカプセル化して保守性を極大化した。<br><strong>[漸進的SPA移行 (Vue 3 + Axios)]</strong> 既存のJSP/JSTLベースの同期式レンダリング画面を、Vue.jsベースの非同期コンポーネントアーキテクチャに全面改修。親子間の単方向データ通信（`props`、`$emit`）による状態制御でモダンなWeb UXを実装した。<br><strong>[サードパーティAPI基盤のドメイン拡張]</strong> Kakao Map APIによる座標変換、Iamportを活用したインライン決済、YouTube APIの非同期検索など、実務で要求される外部インターフェース連携ロジックをVueライフサイクル内に完全に移植した。</li></ul> |
| **07-01** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[양방향 실시간 통신 (WebSocket) 도입]</strong> 단방향 HTTP 요청(AJAX/Axios)에 의존하던 기존 구조를 넘어, `jakarta.websocket` d을 활용한 실시간 채팅(`chat.do`) 아키텍처를 구현함. 서버와 클라이언트 간에 지속적인 TCP 연결을 유지하며 데이터를 Push/Pull 하는 전이중(Full-Duplex) 통신 파이프라인을 체화함. <br><strong>[OpenAPI 기반 데이터 파이프라인 (Naver API)]</strong> 외부 포털(Naver)의 검색 API를 호출하고 Client ID/Secret을 통한 헤더 인증(Authorization) 통신 규약을 구현함. 수신된 비정형 텍스트를 `json-simple` 라이브러리의 `JSONParser`를 활용해 정형화된 객체 배열로 안전하게 파싱하여 클라이언트에 제공함. <br><strong>[단위 테스트(Unit Test)의 시작]</strong> `ReplyDAOTest.java`를 생성하여, WAS(Tomcat)를 구동하고 브라우저에서 화면을 클릭하며 검증하던 기존의 수동 통합 테스트 방식에서 벗어남. 백엔드 데이터 접근 계층(DAO)의 무결성을 독립적인 자바 애플리케이션 레벨에서 즉각 검증하는 테스트 주도(TDD) 기반의 엔지니어링 습관을 확립함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Real-time Full-Duplex Communication (WebSocket)]</strong> Transcended traditional stateless HTTP requests by integrating the `jakarta.websocket` for a real-time chat service (`chat.do`). Established a persistent TCP connection, enabling true full-duplex communication where the server can actively push data to clients. <br><strong>[OpenAPI Data Pipeline (Naver API)]</strong> Implemented a robust data pipeline by calling the Naver Search API with proper HTTP Header authorization. Parsed the raw JSON responses safely using the `json-simple` library, converting external payloads into structured DTOs for the frontend. <br><strong>[Introduction to Unit Testing]</strong> Shifted from manual, browser-based integration testing to automated backend verification by introducing `ReplyDAOTest.java`. Validated DAO layer integrity independently without requiring a Tomcat server startup.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[双方向リアルタイム通信 (WebSocket) の導入]</strong> 単方向のHTTPリクエストに依存していた既存の構造を越え、`jakarta.websocket` を活用したリアルタイムチャット（`chat.do`）アーキテクチャを実装。持続的なTCP接続を維持し、データをPush/Pullする全二重（Full-Duplex）通信パイプラインを体得した。<br><strong>[OpenAPI基盤のデータパイプライン (Naver API)]</strong> 外部ポータル(Naver)の検索APIを呼び出し、ヘッダー認証（Authorization）を伴う標準的な通信規約を実装。受信したJSONテキストを`json-simple`ライブラリを用いて安全に解析し、定型化されたオブジェクト配列としてクライアントに提供した。<br><strong>[単体テスト (Unit Test) の開始]</strong> `ReplyDAOTest.java`を作成し、WAS（Tomcat）を起動してブラウザ上で検証する手動テスト方式から脱却。バックエンドデータアクセス階層（DAO）の整合性を独立したJavaレベルで即座に検証するテスト駆動（TDD）の手法を確立した。</li></ul> |
| **07-02** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[WebSocket 기반 실시간 채팅 로직 고도화]</strong> 기존의 채팅 핸들러 구조를 재점검하여 서버와 클라이언트 간의 메시지 송수신 파이프라인을 최적화함. `jakarta.websocket` 엔드포인트에서 클라이언트 세션을 관리하고, 브로드캐스팅(Broadcasting) 로직을 개선하여 메시지 전달의 지연 시간을 최소화함. <br><strong>[MVC 컨트롤러 로직 최적화]</strong> `BoardModel` 및 각 모델 클래스에서 응답 데이터 생성 시 `Commons.sendData`를 적극 활용하여, 중복되는 `PrintWriter` 호출 및 인코딩 설정을 일원화함. 이로 인해 컨트롤러의 코드가 비즈니스 로직에만 집중하도록 가독성을 대폭 개선함. <br><strong>[데이터 접근 계층(DAO)의 쿼리 안정성 강화]</strong> MyBatis 매퍼(`board-mapper.xml` 등)에서 오라클 페이징 쿼리(`OFFSET-FETCH`)의 무결성을 재확인하고, `SqlSession`의 자원 반납(close) 누락 방지를 위한 예외 처리 패턴을 보완하여 데이터베이스 커넥션 풀(DBCP)의 안정성을 확보함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[WebSocket Chat Logic Refinement]</strong> Refined the real-time chat handler, optimizing the message transmission pipeline between server and client. Enhanced session management and broadcasting logic in the `jakarta.websocket` endpoint to ensure low-latency communication. <br><strong>[Optimized Controller Logic]</strong> Standardized response generation across `BoardModel` and other controllers by leveraging `Commons.sendData`, eliminating redundant boilerplate code and allowing for cleaner, intent-focused controller methods. <br><strong>[DAO Query Integrity Enhancement]</strong> Audited MyBatis paging queries (`OFFSET-FETCH`) for SQL integrity. Strengthened resource management patterns in DAOs to prevent `SqlSession` leaks, ensuring robust connection pool stability.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[WebSocketベースのリアルタイムチャットロジックの高度化]</strong> チャットハンドラー構造を再点検し、サーバー・クライアント間のメッセージ送受信パイプラインを最適化。`jakarta.websocket`エンドポイントでのセッション管理およびブロードキャストロジックを改善し、通信遅延を最小化した。<br><strong>[MVCコントローラロジックの最適化]</strong> 各モデルクラスでの応答生成時に`Commons.sendData`を全面的に活用し、反復的な`PrintWriter`呼び出しを排除。コントローラのコードをビジネスロジックに集中させることで可読性を大幅に向上させた。<br><strong>[DAOクエリの整合性強化]</strong> MyBatisマッパーにおけるOracleページングクエリ（`OFFSET-FETCH`）の整合性を再確認。`SqlSession`のクローズ漏れを防ぐための例外処理パターンを補完し、DBコネクションプールの安定性を確保した。</li></ul> |
| **07-06** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[정적 에셋(Static Asset)의 구조적 통합]</strong> `yummy-master` 템플릿의 방대한 정적 자원(이미지, CSS, JS)을 `JSPLastProject` 내부에 완전히 내재화함. 경로(Path) 라우팅을 최적화하여 뷰(View) 계층의 시각적 완성도와 배포 안정성을 동시에 확보함. <br><strong>[JSP + Vue.js 하이브리드 아키텍처 최종 결합]</strong> 자체 제작한 프론트 컨트롤러(`DispatcherServlet`) 백엔드 시스템과, Axios 기반의 Vue 3 프론트엔드(SPA) 생태계를 완벽하게 결합함. 서버 사이드 렌더링(SSR)과 클라이언트 사이드 렌더링(CSR)의 장점을 융합한 모던 웹 아키텍처를 완성함. <br><strong>[다중 도메인 통합 및 MVC 파이프라인 완성]</strong> 회원, 맛집, 게시판, 예약 등 독립적으로 개발되던 도메인 모듈들을 하나의 거대한 애플리케이션 컨텍스트로 융합함. `Controller -> Service -> DAO -> MyBatis`로 이어지는 4계층 패턴을 전면 표준화하여 유지보수성을 극대화함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Static Asset Structural Integration]</strong> Fully internalized the extensive static resources (images, CSS, JS) of the `yummy-master` template within the `JSPLastProject`. Optimized path routing to ensure visual integrity and deployment stability in the View layer. <br><strong>[JSP + Vue.js Hybrid Architecture Finalization]</strong> Successfully combined the custom-built Front Controller (`DispatcherServlet`) backend with the Axios-driven Vue 3 frontend (SPA). Established a modern web architecture leveraging both SSR and CSR methodologies. <br><strong>[Multi-Domain Consolidation & MVC Pipeline]</strong> Merged independently developed domain modules (Member, Food, Board, Reserve) into a unified application context. Standardized the 4-tier pipeline (`Controller -> Service -> DAO -> MyBatis`) across all domains to maximize enterprise-level maintainability.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[静的アセットの構造的統合]</strong> `yummy-master`テンプレートの膨大な静的リソース（画像、CSS、JS）をプロジェクト内部に完全に内製化。パス（Path）ルーティングを最適化し、View階層の視覚的完成度とデプロイの安定性を同時に確保した。<br><strong>[JSP + Vue.js ハイブリッドアーキテクチャの最終結合]</strong> 独自開発したフロントコントローラ（`DispatcherServlet`）バックエンドシステムと、AxiosベースのVue 3フロントエンド（SPA）生態系を完全に結合。SSRとCSRの長所を融合したモダンWebアーキテクチャを完成させた。<br><strong>[マルチドメインの統合とMVCパイプライン完成]</strong> 会員、グルメ、掲示板、予約など独立して開発されていたドメインモジュールを単一の巨大なアプリケーションコンテキストに融合。`Controller -> Service -> DAO -> MyBatis`へと続く4階層パターンを全面標準化し、保守性を極大化した。</li></ul> |