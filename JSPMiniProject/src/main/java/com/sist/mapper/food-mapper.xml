<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.mapper.food-mapper">
	<select id="foodListData" resultType="FoodVO" parameterType="int">
		SELECT fno,name,poster,address,phone,likecount,replycount,theme,content,type
		FROM food
		ORDER BY fno ASC
		OFFSET #{start} ROWS FETCH NEXT 12 ROWS ONLY
	</select>
	<select id="foodTotalPage" resultType="int">
		SELECT CEIL(COUNT(*)/12.0)
		FROM food
	</select>
	<update id="foodHitIncrement" parameterType="int">
		UPDATE food SET
		hit=hit+1
		WHERE fno=#{fno}
	</update>
	<select id="foodDetailData" resultType="FoodVO" parameterType="int">
		SELECT *
		FROM food
		WHERE fno=#{fno}
	</select>
	<select id="foodNearData" resultType="FoodVO" parameterType="string">
		SELECT fno,name,poster,address,rownum
		FROM food
		WHERE address LIKE '%'||#{address}||'%'
		AND rownum&lt;=7
	</select>
</mapper>